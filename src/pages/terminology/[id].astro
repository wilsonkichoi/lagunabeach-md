---
/**
 * /terminology/[id] — unpublished 2026-06-22, see /terminology/index.astro.
 * getStaticPaths returns [] so no per-term pages are generated; any direct
 * hit falls through to Astro's default 404.
 */
export async function getStaticPaths() {
  return [];
}
---
