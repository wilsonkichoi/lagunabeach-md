---
/**
 * /fork-graph — unpublished 2026-06-22, alongside /terminology. Same problem:
 * a cross-strait Mandarin vocabulary fork visualization (便當 vs 盒飯,
 * 品質 vs 質量, etc.) with no Laguna Beach equivalent. The data and template
 * stay in the repo as legitimate inherited source — just not served.
 */
return Astro.redirect('/');
---
