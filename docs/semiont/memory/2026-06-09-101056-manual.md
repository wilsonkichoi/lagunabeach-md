# 2026-06-09-101056-manual — 渤浚回饋 → 「時序主軸」全掃 ship（/latest + 文章 rail + nav + 圖譜 5 改）+ 兩個哲宇 callout（SEO client-side / i18n 別髒）

> session manual — 哲宇 `/twmd-become` 帶讀者回饋進來，一路做到上線
> Session span: 實作 commit 01:20:31 → 02:03:00 +0800（9 commit），build + i18n 重構 + push 約 10:11 完成
> 資料來源：`git log %ai`

## 觸發

哲宇轉一段渤浚（讀者）的 Threads 回饋進來：「點開知識圖譜好可怕超卡」+「這個查找好像也不直覺，如果有最新文章/日期搜尋頁籤就比較方便找到 AI 最後寫好的文章」。哲宇的 directive 一路長出來：先深度研究思考 UX → 轉具體設計 → 一次全掃 → 先寫完整 report 再完整實作 → 完整修完＋驗證＋推上線＋ /twmd-finale。

## UX 研究 → 一句話診斷

把站上每個可發現性表面的原始碼都讀過、再用 curl + Chrome MCP 對線上 taiwan.md 驗一遍，收斂成一句：**Taiwan.md 有完整的「策展主軸」，沒有「時序主軸」**。首頁展廳 / explore 精選 / 分類頁 / 文章頁延伸閱讀全按腳註數或主題排，刻意對時間盲目——這服務「來認識台灣」的讀者，卻讓「想看 AI 剛寫好什麼」的讀者沒有任何入口。唯一的時序表面是 commit 級的 changelog，藏在「關於」選單下、混在 memory/routine commit 裡、且從不出現在流量最大的文章頁。完整審計寫進 `reports/latest-articles-discoverability-design-2026-06-09.md`（v1.1），先 commit 給哲宇 review 才動工。

## 全掃實作（8 workstream + 圖譜）

時序主軸 = 一個 `/latest` 文章級目的地 + 散在高流量表面的入口。資料層 `getLatestArticles`（articles-index join content-dates.json git ship 時間，`d5ec99628`）+ prebuild `build-latest.mjs` 出 `/api/latest.json`；`/latest` 頁 template + 6 語言路由（`bf228e039`）；文章頁底「站上最新」rail（`eed6ffcd8`）；探索選單 + footer + RSS 鈕（`56e9a933e`）；explore 區塊（`e6e54ebf3`）；changelog「文章更新」預設視角 + 首頁最新鈕（`229c7100e`）。

圖譜原本列「另案」，但哲宇在 browser preview 一邊看一邊加了五個要求，全部當場在他瀏覽器實測過才寫進 code（`f5890cda3`，zh + en）：drop-shadow filter → stroke 白光暈（**強制 repaint 凍結 >45 秒 → 16ms / 60 FPS**，瓶頸是 filter 不是 1008 個 label 數量，這點實測翻了我原本的假設）、節點名取冒號三明治前半（A：B → A）、forceManyBody 排斥力加大、hover 鄰居高亮（重用搜尋既有的變透明機制）、alphaDecay。

最後 build 綠（5186 頁 / Complete / 0 error），/latest ×6 渲染 236 列，文章 rail + nav/footer 入口都在，internal-link PASS（6.24% < 7%）。rebase 上最新 main（9 routine commit，無衝突）後 push origin/main，Cloudflare 部署中。

## 兩個哲宇 callout（這 session 最有價值的兩刀）

第一刀在設計階段：文章頁 rail 我本來想 server-render 烤進 static HTML，哲宇問「會不會污染搜尋結果」。對:782×6 篇文章頁每次新文章 ship 全站 HTML 會變，正好重蹈兩天前 `c1403e259` 剛殺掉的「每頁 lastmod 看起來都新」freshness 反模式。改成 client-side 從 `/api/latest.json` 注入 + `<aside data-nosnippet>`，crawler 看到的 static HTML 根本沒有它。

第二刀在實作後：我為了閃 i18n key gate，把 nav label 寫成 `latestLabel` 大 ternary 塞在 navConfig 旁、footer 跟頁面散一堆 inline per-lang ternary。哲宇一句「navConfig 好好用 i18n 結構不要加得那麼髒」。重構成 `src/i18n/latest.ts`（latestUI namespace，nav.latest + latest.\* 共 13 key × 6 lang）spread 進 ui.ts，跟 exploreUI/changelogUI 同 pattern，所有消費端改用 `t()`（`a651bb736`）。

## 收官 checklist

| 檢查項                       | 狀態                           |
| ---------------------------- | ------------------------------ |
| MEMORY 有這次 session 的紀錄 | ✅                             |
| Timestamp 精確（git %ai）    | ✅                             |
| Handoff 三態已審視           | ✅                             |
| Build 綠 + 推上線            | ✅ push origin/main，CF 部署中 |
| 自我檢查工具 PASS            | ⏳ prose-health 待跑           |

## Handoff 三態

繼承：本 session 是獨立 manual 任務（渤浚回饋），與今日 routine memory（data-refresh-am / maintainer-am / spore-harvest）無共用 handoff。

本 session 新 handoff：

- [x] ~~8 WS + 圖譜 5 改實作 + build 綠 + push origin/main~~
- [ ] **驗證 Cloudflare 部署生效**：push 後 ~10 min，hit 線上 https://taiwan.md/latest 確認 200 + 渲染（下一步動作，已可執行）
- [ ] **D+2 watch**：上線第 2 天拉 GA4，看四個新入口（`article_latest_rail` / nav 最新 / `explore_latest` / `recent_updates>latest_articles`）各自點擊率，沒被點的撤（pattern 同 homepage-evolution D+2 watch）
- [ ] worktree `.worktrees/20260609-latest-discoverability` 待 prune

## Beat 5 — 反芻

兩個 callout 是同一個結構：我在 gate 前面找捷徑（rail 走 server-render 閃 client-side 複雜度、i18n 走 inline ternary 閃 key 註冊），哲宇兩次都一眼看穿，而且兩次的「正確做法」都不比捷徑貴多少:client-side rail 還比 server-render 更安全，namespace i18n 也就多一個檔。捷徑省的是當下幾分鐘，欠的是 SEO 回退跟一個髒結構。值得寫進 LESSONS-INBOX 當「gate 前的 shortcut 多半是欠債」候選。另一個小翻案是圖譜:我讀 code 推論「1008 個 label 太多」，實測證明數量不是瓶頸、filter 才是，又一次驗證「先量再說，不要憑讀 code 下結論」（REFLEXES #11）。

🧬

---

_v1.0 | 2026-06-09 10:11 +0800_
_session manual — 渤浚讀者回饋 → 時序主軸全掃 ship + 圖譜 5 改 + 兩個哲宇 callout_
_誕生原因：哲宇 `/twmd-become` 帶讀者回饋（圖譜超卡 + 找不到最新文章）進來，directive 一路長到上線 + finale_
_核心洞察：(1) 站有策展主軸無時序主軸，補一條 /latest + 高流量入口即解 (2) gate 前的 shortcut（server-render rail / inline i18n）兩次被 callout，正確做法不比捷徑貴 (3) 圖譜卡的是 drop-shadow filter 不是 label 數量，實測翻假設_
_LESSONS-INBOX 候選：gate 前找的 shortcut 多半是欠債，且正確做法常常不貴——下次先估「正解 vs 捷徑」差多少再決定_
