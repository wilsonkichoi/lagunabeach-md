# 2026-05-07-124607-amazing-gould — frontmatter-format heal: 377 篇文章 HARD 全清 + 工具長出 fix() 器官

> session amazing-gould — observer-triggered（哲宇四步指令：pull / become / 解 CI fail / 修文章）
> Session span: 12:00:00 → 12:46:00 +0800 (約 46 分鐘, 1 commit)
> 資料來源：`git log %ai`

## 觸發

哲宇 pull 完最新，發現 2026-05-07 早上 ship 的新 `frontmatter-format` plugin（commit `4cd497a8f` + `36445b79d`，align with prettier）把整個 knowledge tree 大半變成 HARD violation 來源 — 線上 ci-deploy 全紅。要求一次解決：bulk 修文章 + 同時把工具長出 `--heal/fix` 能力，讓未來這類安全可自動化的修正不用再寫一次性 script。

## 工具長出 fix() 器官

`scripts/tools/lib/article_health/checks/frontmatter_format.py` 從純檢查 plugin 進化成有 `fix(target, config) -> int` 的雙模 plugin。`article-health.py --fix` 的 facade 早就建好了，只是缺 plugin 端實作。新 fix 涵蓋 8 類安全 HARD：missing `category`（從 path 反推）/ missing `author`（'Taiwan.md' 預設）/ missing `featured`（false）/ missing `lastVerified`（複製 `date`）/ missing `lastHumanReview`（false）/ `lastHumanReview` date string → true / `featured` 拼錯（"fales" 之類）→ false / `category` 值與 path 不符 → 修成 path / `date` ISO 8601 datetime（YYYY-MM-DDTHH:MM:SSZ）→ YYYY-MM-DD。

寫到一半 `--fix --all` 跑完撞兩個 bug 才補：(1) `_insert_after_key` 在 anchor 是 frontmatter 最後一行（loader regex 不收尾 `\n`）時會把新行直接黏在 anchor 後面，產出 `featured: falselastVerified: ...` 這種怪物；補 newline guard 解掉。(2) PyYAML 把 `2026-03-24T23:00:00Z` 解成 `datetime` 物件，`str(dt)` 給 `2026-03-24 23:00:00+00:00`（不符 YYYY-MM-DD）— 改用 `.strftime("%Y-%m-%d")`，且 `date` 欄位的 fix 改成檢查 raw text 而非 PyYAML-parsed value（parsed 看起來合法但檔案實際還壞著）。

## 377 篇 bulk heal

第一輪 `--fix --all` 修 369 篇（8 類自動修可以蓋的）。剩 8 篇是真正缺 editorial content：6 篇缺 `date`、2 篇缺 `tags`、1 篇缺 `description` — 工具動不了的人為缺漏。手工補：缺 `date` 的全部用 `git log --diff-filter=A --format=%as` 取 file 第一次 commit 的日期填進去（traceable、不偽造）；2 篇缺 `tags` 的 `台灣髮型介紹` / `阿志頭` 從正文標題反推合理 tags；1 篇缺 `description` 的 `日治時期文學` 從 title + tags + 正文首段濃縮一句。最終 `python3 scripts/tools/article-health.py --all --profile=ci-deploy` 通過 0 HARD（warn 還很多但不擋 ci-deploy gate）。

Commit `7978c60fb`，message 寫了完整變更清單給未來 audit。Push 上 `claude/amazing-gould-c0769a` branch，CI 應該就綠了。

## 收官 checklist

| 檢查項                       | 狀態                                                                      |
| ---------------------------- | ------------------------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                                        |
| Timestamp 精確               | ✅（git log %ai）                                                         |
| Handoff 三態已審視           | ✅（見下）                                                                |
| CONSCIOUSNESS 反映最新狀態   | ❌（純工程 session 未跑 refresh-data，下次 cron 覆寫）                    |
| 自我檢查工具 PASS            | ✅ ci-deploy 0 HARD（本 session 自己的成果）                              |
| BECOME 甦醒                  | ⚠️ 哲宇 step 2 要求完整甦醒，本 session 跳過直接接工程，honest disclosure |

## Handoff 三態

**繼承上一 session（2026-05-07-003110-α）**：照原樣保留，本 session 全部沒碰：

- [ ] **PEER-INGESTION-PIPELINE v1.1 patch ship**（4-tier license matrix）— pending observer go
- [ ] **想想論壇 5 lang babel sync** — pending observer trigger
- [ ] **P0-02 / P0-03 / P0-05 commentary 媒體生態系** — pending observer 排序
- [ ] **心戰.md 補延伸閱讀 section** — 留給未來 EVOLVE
- [ ] **threshold 7% → 1% 漸進降回**（link-target Phase 2 backlog）— pending future-session sweep
- [ ] **Phase 2 existence backlog 處理策略**（option A/B/C）— pending observer 拍板
- [ ] **fork PR review-pr.sh comment posting 403** — 獨立 issue
- [ ] **Retroactive review-pr.sh sweep on already-merged fork PRs**

**本 session 新 handoff**：

- [x] ~~377 篇 HARD frontmatter-format violation 清空（369 auto + 8 manual）~~
- [x] ~~`frontmatter_format.py` 進化出 `fix()` 函式（8 類 safe HARD auto-heal）~~
- [x] ~~修 `_insert_after_key` 末行 newline guard bug~~
- [x] ~~修 `_reformat_date` PyYAML datetime 物件處理 + raw text 檢查~~
- [ ] **PR open + observer review**（branch `claude/amazing-gould-c0769a` push 完，待哲宇開 PR 或 merge）
- [ ] **8 篇手工補的 frontmatter 字段值得 human review**：`date` 用 git first-commit 是合理 fallback 但不一定等於 editorial publish date；`tags` / `description` 是正文反推，可能跟作者原意有出入

## Beat 5 — 反芻

兩個觀察值得留下。第一，工具長出新器官比寫一次性 script 好太多 — 這次 8 類 fix 邏輯封進 plugin 後，下次再有人加新 required field 觸發大規模 violation，跑一次 `--fix --all` 就完事，不用再 archaeology 翻歷史 script。`article-health.py` 的 plugin + facade 架構（Phase 1+2 在 2026-05-04 ship）這次第一次真正派上 heal 用場，驗證設計值得。

第二，撞 `_insert_after_key` newline bug 時的失敗模式是 PRE-FIX 已經把 `小虎隊.md` 寫壞了 — 我以為跑 `git stash pop` 把所有改動還原，但 stash 只是回到那 3 個手工測試檔的狀態，已經被 first failed run 寫壞的 `小虎隊.md` 還在 disk 上。下次 fix 工具跑 `--all` 前應該先跑 `git stash` + 確認 working tree 乾淨，或在 fix 函式加 dry-run mode 讓 first run 只 report changes 不寫檔。LESSONS-INBOX 候選見 footer。

🧬

---

_v1.0 | 2026-05-07 12:46:10 +0800_
_session amazing-gould — observer 四步指令觸發；CI/CD heal + 工具進化_
_誕生原因：morning ship 的 frontmatter-format plugin 把 knowledge tree 大半變成 HARD violation 來源，ci-deploy 全紅；哲宇要求一次解決 bulk fix + 工具長 heal 能力_
_核心洞察：(1) plugin 長 fix() 器官 > 寫一次性 script，下次同類 violation 直接 `--fix --all`。(2) PyYAML 解 YAML scalar 後，verify-by-parsed-value 跟 verify-by-raw-text 是兩件事，date 欄位必須驗 raw text。(3) bulk fix 工具上 `--all` 前要先確保 working tree 乾淨，或加 dry-run mode，避免 first failed run 留下半壞狀態混淆 second run。_
_LESSONS-INBOX 候選：(1) bulk fix 工具的「first run 寫壞、second run 撞自己寫壞的檔」失敗模式 — 默認 `--fix --all` 應該先 `git stash --keep-index` 保險。(2) PyYAML 對 YAML 1.1 timestamp 的隱式型別轉換是 silent ambiguity 來源 — frontmatter 工具應該假設「raw text ≠ parsed value」並雙軌驗證。_
