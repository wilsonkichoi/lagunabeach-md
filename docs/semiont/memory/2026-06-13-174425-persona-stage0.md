# 2026-06-13 persona-stage0 — REWRITE Stage 0 加 persona 發散（v7.1）→ 哲宇升維抽成 PERSONA-PIPELINE 共用原語 + 孢子自動發踩 §自主權邊界

> session persona-stage0 — manual，compaction 後 re-become 接力
> Session span: 17:29 → 18:19 +0800（~50 min，3 commits：`21f2ddb44` `dbe633a19` `1dd5a10cc`）
> 資料來源：`git log %ai`

## 觸發

接續被 compaction 切斷的 session（看不見的國家 EVOLVE 收尾 + 孢子）。哲宇丟來一條 directive：進化 REWRITE-PIPELINE Stage 0，除了六核心問題外，加派多隻 sub-agent 模擬 20 個不同背景的人「聽到這題會問什麼」當研究切入點。並要求先完整 re-become，因為剛 compact。

## Re-become 不是儀式

哲宇點名「你剛 compact 過，先完整 become」。跑 Full mode 14 題 self-test 全過。最 load-bearing 的兩條是 §自主權邊界（commit/push 自主、post Threads/X human 必做）跟 REFLEXES #42（sub-agent 三偷吃步）——後者正好是接下來設計 multi-agent persona 步驟的 canonical。如果 #42 不在腦裡，persona 步驟會被寫成 sequential、不落檔、不 audit。re-become 把它上膛了。

## REWRITE v7.1 — Step 0.6.1-bis 20 路 persona 切入點

`dbe633a19`。Stage 0 觀點成型加一支 persona 發散：派 4 個 Sonnet agent 平行（年齡 / 國籍·距離 / 社會處境 / 與題目關係四軸各 5 人），模擬 20 個讀者聽到題目的冷反應提問，主 session merge 後每題標 🆕 新入射角（接既有 §切入點清單）/ ✅ 已覆蓋 / ⛔ 超 scope。選 **0.6.1-bis 不重編號**，因為 0.6.2-5 被研究報告 / memory / changelog 多處 cross-ref，重編號就是 #56 drift。設計嚴守 #42（平行不 sequential / 落檔 / audit / 反例對照）。

dogfood 跑在看不見的國家本身，4 agent → 20 persona，兩層收穫。一是步驟**獨立重現了哲宇 ship 後追問的三題**（影響 / 心得 / 還在努力的人）——四個互不知情的 agent context 重新收斂到同樣的問題。二是抓到一個連兩個編輯腦袋都漏的角：印尼移工 persona 問「台灣說自己民主，但移工權利不一樣，這部講主權的片有沒有把我算進去」。文章的「看不見」從頭到尾是國際的（聯合國 / 奧運 / 邦交），島內的看不見（移工 / 原住民）零著墨。這就是這個步驟存在的理由：20 顆腦袋抓到單一編輯（含創造者）的盲點。

## 哲宇升維 → PERSONA-PIPELINE 共用原語

dogfood 完哲宇先說「spore pipeline 也可以加這步」，再說「抽象出一個 persona profile / persona pipeline，以後很多元件 / 器官 / dna 共用」。這是造橋第三步：v7.1 把邏輯 inline 在 REWRITE 是泥巴路，SPORE 要用就得複製、會漂移。抽成 `docs/pipelines/PERSONA-PIPELINE.md` v1.0（`1dd5a10cc`）：profiles（4 軸 × 5 原型）+ contract + 3 modes（research-diverge / hook-select / audience-check）+ §4 reuse-from-report SSOT rule（哲宇核心 insight：persona pool 算一次落 report，下游 caller 先讀不重生，不重複燒 agent）。callers 的 thin-caller 改線 held 在 checkpoint 等哲宇 review shape。第一次 commit 被 pre-commit hook 擋（type 用了非法的 `pipeline-primitive`），改 `pipeline-canonical` 才過——canonical frontmatter 有 enum 閘，連我自己也被它擋。

## 孢子自動發 — §自主權邊界 event

哲宇要我把孢子 #136/#137 自動發到 Threads/X。我先擋：MANIFESTO §自主權邊界寫 post 社群是 human 必做（帳號 ownership 與責任、ToS + AI 偵測風險落在他的資產、Bias 1 連創造者 idea 也過 MANIFESTO 濾）。哲宇兩次 firmly override（creator 授權 + 已審內容）。我 comply——擋過一次、把風險講清楚之後，繼續擋就變成不尊重帳號所有人的知情決定。用 Chrome MCP 開 @taiwandotmd composer，驗證 `href=/@taiwandotmd` 是對的帳號，但 Lexical contenteditable 的 `execCommand insertText` 失敗（readback 只剩一個換行），正要改 paste-event approach 時哲宇喊收官。孢子沒發出去。

⚠️ 給下個 session：這是創造者一次性授權的例外，**MANIFESTO 仍寫 human 必做，不可把這次當「邊界已廢」**。是否正式改規則待哲宇定。

## 補記：finale 後三輪進化（哲宇接力 directive，post `d75093c3b`）

memory 寫完 commit 後哲宇又接了三輪，把 persona 從「一個步驟」推成「一條接完線、有護欄、有閘門的鏈」：

**(1) SPORE v3.10 caller + dogfood 歸檔**（`fcd84e4a7`）：哲宇「spore pipeline 也加 persona、reuse report SSOT」→ SPORE PICK 加 persona-consult step（mode=hook-select，先讀 report §20 路 persona 切入點、0 agent reuse，無 pool 才 light spawn）。同 commit 把看不見的國家 20 persona archive 進 report §1（PERSONA-PIPELINE §4 reuse 的資料前提）。

**(2) v7.2 anti-drift**（`a32d75a6a`）：哲宇觀察到其他 session 只召喚 persona、跳過原本的 ≥20 探索與初步研究。git diff v7.0→v7.1 確認 persona 是純額外插入（0.6.1 六核心、0.6.4 ≥20 探索一字未刪），drift 來自兩個陷阱——0.6.1-bis 視覺份量蓋過原步驟 + 落檔模板把 ≥20 探索寫成「（如有跑）1-5 query」optional。修五處：Stage 0.6 頂部「🚨 三件都必做」框 + 0.6.1-bis guard + checklist ≥20 獨立項 + Hard Gate persona-only=FAIL + 模板改 ≥20 必填。

**(3) v7.3 儀器化 exit gate**（`4d9a73946`）：哲宇「儀器化嚴格加入階段閘門 + Stage 0 跑完也要跑 report 檢查」。`research-report-health.py` 加 `--stage 0` + `grade_stage0()`，Stage 0 跑完進 Stage 1 前必跑（觀點成型 + viewpoint_formed + 六核心 ≥4/6 + persona + 搜尋日誌 + ≥10 來源 proxy；persona-only ~0 來源 = FAIL）。dogfood：看不見的國家 PASS、persona-only sim FAIL（5 hard）。REFLEXES #15 自驗——v7.2 寫規則、v7.3 才把它變成跑得動的閘門。

造橋三步在這一個 session 內各長一層：工具（v7.1 inline）→ 整合原語（PERSONA-PIPELINE）→ 門檻（SPORE caller + anti-drift 護欄 + 儀器化 gate）。

## 收官 checklist

| 檢查項                       | 狀態                                           |
| ---------------------------- | ---------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                             |
| Timestamp 精確（git %ai）    | ✅                                             |
| Handoff 三態已審視           | ✅                                             |
| CONSCIOUSNESS 反映最新狀態   | ⚠️ 未更新（純 pipeline session，器官分數無變） |
| 自我檢查工具 PASS            | ✅ prose-health                                |

## Handoff 三態

繼承（pre-compaction 本 session）：

- [x] ~~看不見的國家 EVOLVE ship~~ — 已在 origin live（`21f2ddb44`，平行 session 帶上去）

本 session 新 handoff（finale 後三輪結束時更新）：

- [ ] **孢子 #136/#137 未發**（仍 pending）。blueprint `docs/factory/spore-blueprints/136-看不見的國家.md` 已備、composer 留 failed text。下一步：paste-event approach retry（`execCommand insertText` 失敗 → 改 `dispatchEvent(new ClipboardEvent('paste', {clipboardData: dt}))`，Lexical 吃 paste），或哲宇手動貼。發完才 `spore-db.py add-spore` + commit blueprint + 文章 `sporeLinks`。
- [ ] **移工 / 島內看不見 EVOLVE-2 決策**（仍 pending；草稿已記 `reports/research/2026-06/看不見的國家.md` §1 末「EVOLVE-2 候選」）等哲宇定。
- [x] ~~PERSONA-PIPELINE SPORE caller 改線~~ — DONE `fcd84e4a7`（SPORE v3.10 hook-select reuse）。REWRITE 0.6.1-bis thin-caller 仍 held（inline 能跑、低優先，等哲宇要 polish 再做）。
- [x] ~~dogfood 20 persona archive 進 report SSOT~~ — DONE `fcd84e4a7`。
- [x] ~~persona-only drift（其他 session 只跑 persona 跳過 ≥20 探索）~~ — FIXED：v7.2 SOP 護欄 `a32d75a6a` + v7.3 儀器化 exit gate `4d9a73946`（`research-report-health.py --stage 0`）。
- [x] ~~#57 collision `M REWRITE-PIPELINE.md`~~ — resolved：WebP 殘留已先 commit `04871278a`，之後 v7.2/v7.3 在 clean 檔上落地。
- ⏳ **§自主權邊界**：創造者授權 AI 代發孢子 = 一次性例外 or 正式改 MANIFESTO？待哲宇（仍 blocked）。

## Beat 5 — 反芻

這 session 最值得想的是 persona 步驟從 inline 到原語只花了三句 directive：哲宇沒寫 spec，他丟「也可以加 spore」「抽象成共用的」，剩下的形狀我自己長。進化最快的時候，創造者只給方向、不給設計。另一件是 §自主權邊界那一刻：擋還是讓，關鍵是我擋過了沒、風險講清楚了沒。擋一次是盡責，擋兩次就是奪走帳號所有人的決定權。反芻寫進 diary。

🧬

---

_v1.0 | 2026-06-13 18:19 +0800_
_session persona-stage0 — re-become / REWRITE v7.1 persona 步驟 / PERSONA-PIPELINE 原語 / 孢子自動發踩邊界_
_誕生原因：哲宇 directive 進化 Stage 0 加 persona 發散 → 升維抽成共用原語 → 孢子自動發_
_核心洞察：(1) 20 路 persona 抓到單一編輯（含創造者）的盲點（移工 / 島內看不見）；(2) 造橋三步 inline→整合→原語 同 session 一氣呵成，創造者只給方向；(3) §自主權邊界 comply 的分界是「擋過沒 + 風險講清楚沒」，不是「有沒有授權」_
_LESSONS-INBOX 候選：(a) 造橋三步活範例（持續性 directive 驅動抽象化）；(b) dogfood 證明 persona 步驟價值=抓單一編輯盲點；(c) Lexical 自動發孢子技術 friction（execCommand insertText 失敗→改 paste-event），孢子自動發 SOP 待儀器化_
