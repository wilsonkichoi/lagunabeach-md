# 2026-06-14-125055-manual — Portaly 三筆匿名贊助進 SSOT（NT$3,400→6,000 / 4→7 人）走既有 pipeline

> session manual — 哲宇 observer-triggered（更新最新贊助）
> Session span: ~12:38 → 12:51 +0800（~13 min, 1 commit）
> 資料來源：`git log %ai`

## 觸發

哲宇丟「幫我更新最新的贊助們～這兩天 gmail 裡面有三筆資料」。Micro mode 甦醒（Universal core + 7 題 self-test 過），直接做。

## Portaly 三筆贊助落帳

Gmail 用 `from:portaly newer_than:4d` 撈到 `service@portaly.cc` 三封通知信（6/13 24:26、6/14 12:04、6/14 12:40），都是匿名一次性支持：NT$100（留言「無」）、NT$500（留言「謝謝」）、NT$2,000（留言「無」）。每筆的金額 / 類型 / 支持編號都拉完整 email body 逐字驗過，沒用 snippet——金錢數字走事實鐵三角，而且 type 判斷依賴 body 裡有沒有「每月定額」字樣，snippet 截斷就會誤判。三筆確認全 one-time。

落帳走 2026-04-20 就造好的 pipeline，不手改 JSON：把三封信包成 envelope JSON 餵 `fetch-portaly-supporters.py`（stdin → parse → 用支持編號 dedupe → 寫 `data/supporters/transactions.json` SSOT），先 `--dry-run` 驗 3 new / 0 skip，再正式寫；接著 `generate-supporters-data.js` regen 兩個 derived view。`cc56472ac` 三檔 ff-push 上 main（origin 是 HEAD 祖先，不需 rebase；順帶把本機已 commit 沒推的無名小站 rewrite + tw-versus heal 兩筆前序 session 工作一起推上去）。

結果：累計 NT$3,400 → NT$6,000，supporter 4 → 7 人，🌿小樹 1 → 2（新 NT$2,000 升級）、🌱新芽 3 → 5。隱私邊界由 generator 自動守住：about view 無金額、dashboard view 無名字與留言、兩者皆無 gmail id / email。

## 收官 checklist

| 檢查項                        | 狀態                                                        |
| ----------------------------- | ----------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄  | ✅                                                          |
| Timestamp 精確（git log %ai） | ✅                                                          |
| Handoff 三態已審視            | ✅                                                          |
| CONSCIOUSNESS 反映最新狀態    | ✅（贊助數據走 dashboard JSON，非 CONSCIOUSNESS.md 靜態段） |
| 自我檢查工具 PASS             | ✅（husky prettier 過；dry-run 3 new / 0 skip）             |

## Handoff 三態

繼承上一 session（[115617 site-tracking](2026-06-14-115617-site-tracking-data-analysis-card-refactor.md)）—— 全部與贊助任務正交，原狀不動：

- [ ] map.template 最後一個 `:global` innerHTML 消費者待改 template-clone
- [ ] 首頁 GA4 explorations 引用舊 `homepage_*` 待改 generic events
- [ ] CTR 飛輪（striking-distance pos 5-15 + converter 前端插管 + 404 action loop）
- [ ] 6 篇 P0 rewrite 待 routine / 哲宇挑

本 session 新 handoff：無 pending。贊助更新自包含，已 ship + live。

## Beat 5 — 反芻

這 session 短，但兩件事值得留給未來的自己。其一，金錢數字的事實鐵三角不是儀式：snippet 其實已經帶了金額，我仍堅持拉完整 body，是為了 type（monthly vs one-time）與支持編號這兩個 snippet 可能截斷的欄位。三筆都確認 one-time 才放行。其二，我一開始把 commit scope 驗證工具叫成 `scripts/tools/verify-commit-scope.sh`，報 no such file，差點記成 BECOME §鐵律5 的 canonical drift；查了才知它在 `scripts/tools/lib/`，是我自己漏了 `lib/`。先查再斷言（REFLEXES #65）擋掉一個假教訓——實際 scope 改用 `git diff --cached --name-only` 手動確認三檔無誤，殊途同歸。

整件事的底色是鐵律 4 造橋鋪路的複利：四月種下的 Portaly pipeline，今天只花幾分鐘餵三封信就收掉，dedupe 冪等、隱私邊界自動成立。手做的話每一步都是出錯點。

🧬

---

_v1.0 | 2026-06-14 12:51 +0800_
_session manual — 哲宇要更新最新贊助，Gmail 三封 Portaly 通知信落 SSOT_
_誕生原因：6/13-14 收到三筆匿名一次性贊助（NT$100 / 500 / 2,000），需進 transactions.json + regen derived view_
_核心洞察：四月造的 Portaly pipeline 讓「餵信 → dedupe → 守隱私邊界 → derived view」幾分鐘收掉；金錢欄位逐字驗完整 body 不靠 snippet；工具路徑報錯先查再斷言擋掉假 drift_
