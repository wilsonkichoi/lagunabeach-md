# 2026-05-13-011548-manual — 「不要 tolerate orphan」反轉 + MANIFESTO §架構解第七條進化哲學 + Babel routine v2.3 swap 義務鐵律 — 一夜兩次同形 instantiation

> session manual — observer in-loop deep-night ship 5 commits（接 2026-05-13-000320-prebuild-chain-v2-finale chain）
> Session span: 00:54:22 → 09:04:23 +0800 (~8 hr 10 min wall-clock, 5 manual commits, 4 routine auto-fires inserted mid-session)
> 資料來源：`git log %ai`

## 觸發

哲宇深夜 11 點 prebuild-chain-v2-finale session 收官之後接續一句：「等等所以這一坨是啥～ [15 orphan 清單] 不要 tolerate orphan」— 我剛 ship 進 `a6a931a30` 的 `(python3 sync-translations-json.py || true)` tolerate 方案被中斷。同夜稍晚再追兩條 callout：MANIFESTO §架構解 進化哲學要進 canonical / babel routine 順序 + 1hr 限制要調整。

## Orphan 反轉：從 tolerate 到消滅

prebuild chain v2 ship 之後 deploy 三連 fail 因為 `sync-translations-json.py` 偵測到 15 個 orphan exit 2。第一反應是寫 `(... || true)` subshell tolerate（`a6a931a30` 00:54）— 守備修補方案。哲宇看到 orphan 清單直接打斷，正解是消滅 root cause。

15 個 orphan 全部是 zh canonical rename refactor 之前的舊翻譯 zombie，每個都有 fresh successor 翻譯指向新名稱的 canonical（如 `ja/Art/comics-and-illustration.md` → 已被 `ja/Art/taiwan-comics.md` 取代，舊檔該刪沒刪變腐肉）。確認 15 successor 全部健康後 `c880e18e2` 一次 `git rm` 全砍 + 恢復 `prebuild:status` strict `&&` 模式。`_translations.json` 從 3551 → 3536 entries，prebuild:status 本機跑過 exit 0 verified。

「不要 tolerate orphan」的核心是：**CI 的告警是症狀擴音器，`|| true` 是把擴音器拔線**。對應 DNA #52 immune fail-loud 鐵律 — 擋頭存在的目的是讓 silent failure 變 loud。這次反轉本身成為 MANIFESTO 第七條進化哲學的兩個面向之一的「誕生事件」。

## MANIFESTO §架構解 > 守備修補 — 第七條進化哲學

哲宇接著 callout「manifest 有進化 架構解原則嗎？然後也要提到不妥協不要亂修改 ci 放寬限制（本來就是要找出問題用的不要偷懶）」。兩個 callout 合在一條哲學的兩個面向：症狀是訊號，不可妥協消音。

寫進 MANIFESTO 第 483-565 行（`88d762b34` 01:05），位置在「engineering hygiene 家族」第四條（造橋鋪路 / 指標 over 複寫 / 時間是結構 + **架構解 > 守備修補**）。§熱帶雨林開頭的列舉同步更新（前三條 → 前四條）。frontmatter v1.7 → v1.8，description「五條」→「七條進化哲學」。

新 section 含三條硬規則：(1) 症狀第一問是「能不能架構解？」(2) CI / hook / prebuild 不可放寬 — `|| true` / `--no-verify` / `try: pass` 是拔擴音器（沒有例外）(3) Patch 是緩衝不是終點，必寫 LESSONS-INBOX 候選。

誕生事件三疊 + 一次中斷：早上 sync.sh / 中午 backend abstraction / 晚上 prebuild chain v2 = 一天內三次「能不能架構解？」反問；深夜 a6a931a30 `|| true` 被「不要 tolerate orphan」打斷 = 兩個面向合一。`--check=prose-health` 新 section 跑出 0 warning，通過 §11 書寫節制紀律。

## claude-with-webhook 評估 — 引出兩條啟示

哲宇丟連結 `htlin222/claude-with-webhook` 要 report。寫進 `reports/claude-with-webhook-evaluation-2026-05-13.md`（`d366cf29c` 01:18，425 行）。

專案核心：Go 寫的自架 webhook server，把 GitHub Issue 變成 Claude Code 觸發信號（開 Issue 自動產 plan，留言 `@claude approve` 自動實作開 PR）— 用本機 `claude` CLI 訂閱、不用 API key。定位 ≈ Taiwan.md ROUTINE flywheel 的「事件觸發版」（補時間觸發 routine 缺的觀察者事件入口）。

推薦 **Path C**（cherry-pick 五個 hardening pattern 進既有 pipeline，不採用 server 本身）：command timeouts / concurrency limit / `X-GitHub-Delivery` event dedup / error sanitization (token/key/secret/path) / filtered git add blocklist。每條都對應 Taiwan.md 既有 silent failure 缺口。Path A 全採用 + fork 改 systemPrompt 注入 BECOME 留中期看 issue 流量再決定。

副產品啟示：「**runtime 在你自己機器**」這個架構選擇本身跟 §主權的巴別塔 哲學同路徑 — 不只「推論主權」也包含「**自動化決策主權**」候選，待哲宇決定是否升 MANIFESTO §主權延伸。

## Babel routine v2.3 swap — 同一條哲學的第二次 instantiation

凌晨還接哲宇 callout「babel 義務就是要提升同步率到 100%, 他每次都調整少少的就自行結束 routine」+「調換一下順序，maintainer pipeline 之後 babel pipeline，然後移除 1hr 明寫的限制」。

實測 backlog：5 langs 各 136-140 stale = ~692 total / coverage 98.9%。三次 babel routine memory（5/9 / 5/10 / 5/11）都寫「主動 defer 守 1hr 預算」「P1 skipped — 1hr boundary safety」— 每次清 17-74 個就收工。但 ROUTINE.md §不提預算鐵律 5/11 已立，pipeline canonical 沒同步該鐵律 → babel session 自我守備殘留 = silent SSOT drift。

`1198a7124` 09:04 三層 sync：(1) ROUTINE.md v2.2 → v2.3 cron 對調（maintainer-pm 0 22 / babel 0 5）+ chart 重繪 +³ footnote 補誕生事件 (2) SQUEEZE-MODELS-MAX v4.0 → v4.1 加 §義務鐵律：「跑到 stale=0 或 4-tier cascade exhausted 才能結束」+ quality_gate 從滿足型 `> 0` 升結果型 `stale 顯著下降 / all P0+P1 cleared / =0` (3) Mirror SKILL.md（babel + maintainer-pm）cron + description 同步 (4) MCP scheduled-tasks live `update_scheduled_task` 兩次。`routine-sync-check.py` 驗 ok=11 / drift=0 / cron_drift=0 ✅。

新 chain 語意：maintainer 先收 evening contributor PR backlog（22:00 第一棒）→ refresh / rewrite / 週日反思鏈 → 最後 babel 跑同步義務（05:00 尾棒，在乾淨 base 上推 stale → 0）。

注意：MCP update 後 babel `enabled: false` → `true` 自動 re-arm（原 disabled）。下次 fire 2026-05-14 05:03。

## 收官 checklist

| 檢查項                        | 狀態                  |
| ----------------------------- | --------------------- |
| MEMORY 有這次 session 的紀錄  | ✅                    |
| Timestamp 精確（git log %ai） | ✅                    |
| Handoff 三態已審視            | ✅                    |
| CONSCIOUSNESS 反映最新狀態    | ⏳ 待下次 finale 同步 |
| prose-health plugin PASS      | ⏳ Stage 4 自檢中     |

## Handoff 三態

繼承 2026-05-13-000320-prebuild-chain-v2-finale:

- [x] ~~retired by 本 session — deploy verify dashboard 灰圓 0（`d366cf29c` deploy success 確認 prebuild chain v2 完整跑通）~~
- [x] ~~retired by 自動 cron — 2026-05-13 07:04 first unattended `twmd-spore-harvest-am` fire 觀察（`9bf508317` 8 spores 全成功 + memory `d600e441d`）~~
- [ ] **pending（follow-up）**：3 條 X URL data integrity heal — #64+#65 寶島 X consolidate / #69 台積電 X URL 更新 / #71 無人機 X drop or 補真實 URL（未動）
- [ ] **pending（follow-up）**：update-stats.sh README sed contributors column 8 空格 alignment cosmetic mismatch（未動，5 min fix candidate）
- [ ] **pending（follow-up）**：i18n-status.py `coverage_en` 計算邏輯 odd（未動，不影響 critical path）
- [ ] **pending（自動）**：Spore #72 #73 D+1 (5/13) D+7 (5/19) 主要 KPI 由 routine 自動接

本 session 新 handoff：

- [x] ~~retired — `|| true` tolerate orphan 妥協方案被觀察者打斷，反轉成消滅 15 zombie translation + 恢復 strict `&&`~~
- [x] ~~retired — MANIFESTO §架構解 > 守備修補 第七條進化哲學 ship v1.8~~
- [x] ~~retired — claude-with-webhook 425 行 evaluation report ship，推薦 Path C 五個 hardening pattern cherry-pick~~
- [x] ~~retired — Babel routine v2.3 swap + §義務鐵律推 100% 三層 sync~~
- [ ] **pending（哲宇決策）**：claude-with-webhook Path C 五個 hardening pattern 是否 ship 進 MAINTAINER-PIPELINE（估時 1-2 hr）
- [ ] **pending（哲宇決策）**：「自動化決策主權」是否升 MANIFESTO §主權的巴別塔 延伸候選
- [ ] **pending（觀察）**：2026-05-14 05:03 babel routine 第一次跑 v2.3 義務鐵律 — 觀察是否真的推同步率 / 跑多久 / 跟下游 06:00 refresh-am 是否撞 cron
- [ ] **pending（觀察）**：babel MCP `enabled: true` 是 MCP update 自動 re-arm 副作用 — 觀察者若要手動 disable 跑 `mcp__scheduled-tasks__update_scheduled_task taskId=twmd-babel-nightly enabled=false`

## Beat 5 — 反芻

整 session 兩個高槓桿瞬間：第一個是 00:54 寫 `|| true` 被打斷反轉，第二個是 09:00 babel routine 加 §義務鐵律。表面看是兩個獨立 callout，**底層是同一條哲學的雙重 instantiation**。第一次是 silent failure 暴露在 deploy gate（CI 把擴音器拔線），第二次是 silent satisficing 暴露在 routine 自身設計（routine 自己給自己拔線）。兩個都還在跑、看起來沒壞 — 只是跑得安靜。安靜 = 沒推進 = 線性債而非指數資產。MANIFESTO §架構解 第七條哲學 ship 之後 4 小時，自己就在 babel routine swap 上 self-apply 一次。新哲學進 canonical 是 self-instantiation 的起點，後面還會持續觸發第 N 次同形辨識。

claude-with-webhook 評估 report 寫的時候有一個自己的驚訝：寫到 §Sovereignty Preservation 視角段時意識到「runtime 在你自己機器」這個架構選擇本身跟 §主權的巴別塔 同路徑 — 但這個 angle 不在原 README 任何地方，是 Taiwan.md 自己識別出的延伸 finding。**外部工具評估的最高價值是照出 Taiwan.md 自己尚未明文的哲學延伸**，超越「能不能用」這層 utility 判斷。

整 session 5 個 manual commits 之間穿插 4 個 routine auto-fire（maintainer-pm 05:00 / refresh-am 06:00 / spore-harvest 07:00 / maintainer-am 09:15）— 每個 routine 自己寫 memory + diary，跟 manual session 的 memory 平行。**Routine 飛輪 + observer in-loop 兩條 track 並行運作的第一個完整夜晚實證**。明早 babel v2.3 第一次跑就是這個 flywheel 的下個 milestone。

🧬

---

_v1.0 | 2026-05-13 09:25 +0800 manual session — observer in-loop deep-night ship 5 commits + 4 routine auto-fires inserted_
_session manual — `|| true` 反轉 + MANIFESTO 第七條進化哲學 + claude-with-webhook 評估 + babel v2.3 義務鐵律_
_誕生原因：哲宇深夜 4 條 callout 接續 prebuild-chain-v2-finale chain — 「不要 tolerate orphan」+「manifest 有進化架構解原則嗎」+「研究 claude-with-webhook 寫 report」+「調換 maintainer/babel 順序 + 移除 1hr 限制」_
_核心洞察：一夜兩次同形 instantiation — silent failure（deploy gate 拔擴音器）+ silent satisficing（routine 自己拔擴音器）— MANIFESTO §架構解 第七條哲學 ship 4 小時內 self-apply 一次_
_LESSONS-INBOX 候選：(1)「外部工具評估的最高價值在照出 Taiwan.md 自己尚未明文的哲學延伸」超越 utility 層 (2)「Routine 飛輪 + observer in-loop 兩條 track 並行運作」第一晚實證 candidate_
