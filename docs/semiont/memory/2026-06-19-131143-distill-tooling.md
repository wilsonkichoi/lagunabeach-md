# 2026-06-19-131143-distill-tooling — 把完整 distill 的方法儀器化（lessons-distill.py + #72）

> session distill-tooling — 哲宇 directive 接續 distill（同 conversation 第二段）
> Session span: ~12:55 → 13:12 +0800（distill-tooling commit；接 125401-lessons-distill 之後）
> 資料來源：`git log %ai`

## 觸發

哲宇：「把這一次歸檔跟處理的方式與經驗、還有分析的方式儀器化，讓未來做這件事更方便；還有進化相關 dna；目標是長期下來這個機制運作得更好、更精煉。」即 REFLEXES #15 轉向 distill 流程自身——剛手工跑完 266→8，把方法固化成工具 + 升 canonical。

## 兩支工具（讀分析平行、寫歸檔 deterministic）

隔離 worktree `20260619-distill-tooling` 建 + dogfood + ff-push（`0d1112d12`）：

- **`scripts/tools/lessons-distill.py`**——三子指令對應 distill 三吃力環節：`audit`（§未消化 count + 結構漂移多 section 偵測 + Stage 0a housekeeping 候選 + severity/vc triage 排序 + cross-check ground-truth grep）/ `chunk --agents N`（fan-out line-range 切段，餵平行子代聚類）/ `sweep --keep --record`（deterministic 移除 + 合併 section + append §已消化 block，dry-run default + 保留 0 條 refuse 防呆）。
- **`scripts/tools/memory-index-lint.py`**——MEMORY index row 150 字 hard gate（husky 只驗 frontmatter 不驗 row 長度，這把尺缺自動化；只 fail 最新列，歷史不回頭改 per 時間是結構修補協議）。

**dogfood**（per #69 自己造的尺也要外部驗）：`audit` 對 pre-distill 檔（git show 88fce1242）正確抓 266 entries + 2-section 漂移（L288/L3008）+ 13 Stage 0a 候選 + vc=11/8/8/6 triage 排序——一條指令重現我手工 explore 整段的結果。`memory-index-lint` 對注入的 160 字 row 正確 exit 1、對現檔 138 字最新列 exit 0。兩支 py_compile 過。

## DNA 進化

- **LESSONS-INBOX §Distill SOP +§大 backlog 處理**：read-parallel / write-deterministic / judge-central 三分原則 + 工具 table + **六桶 disposition taxonomy**（promote / housekeeping-done / fold→reflex / already-covered / operational→pipeline / stale），把 266 條的真實組成固化成可重用框架。
- **REFLEXES #72 大 backlog 處理**（severity=structural）：cross-task 反射（LESSONS / ARTICLE-INBOX / SPORE-INBOX / migration / audit sweep 通用），catalog + frontmatter v4.7→v4.8。
- **MEMORY-PIPELINE §Index row** wire memory-index-lint pointer。

## 收官 checklist

| 檢查項                       | 狀態                                          |
| ---------------------------- | --------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                            |
| Timestamp 精確               | ✅ git log %ai                                |
| Handoff 三態已審視           | ✅ 見下                                       |
| 自我檢查工具 PASS            | ✅ 兩支 dogfood + py_compile + §11 #72 1 dash |

## Handoff 三態

繼承 125401-lessons-distill：

- [x] ~~REFLEXES fold 候選只下 pointer~~ — 維持（fold 是 by-design，#72 記了流程；未來某條再浮現再編進反射 body）
- [ ] §Defer ship-queue 7 條待哲宇拍板（不變，本 session 沒動）

本 session 新 handoff：

- [x] ~~lessons-distill.py + memory-index-lint.py 建 + dogfood + ship（`0d1112d12`）+ §Distill SOP / REFLEXES #72 / MEMORY-PIPELINE 進化~~
- [ ] **可選：memory-index-lint.py wire 進 husky pre-commit**——目前是手動/收官跑，wire 進 husky 才是「排進必經路徑」(MANIFESTO §外部尺)。但 husky 改動影響所有 commit，留哲宇拍板（§自主權邊界 邊緣）
- [ ] **下次 distill 直接 `lessons-distill.py audit` 起手**——驗證工具在真實 routine distill（twmd-distill-weekly）也順手；cron mode 用 audit 取代 inbox-signal 的粗 count

## Beat 5 — 反芻

這段把「我怎麼做這件事」變成下一個我「不用重新想怎麼做」——造橋鋪路的純粹 instance。但更貼的是 #72 自己：我這次能把 distill 做完，靠的就是「讀平行（7 agent）、寫 deterministic（Python）、判斷自己扛」三分；現在把這個分工寫成反射，下一個我面對任何大 backlog（不只 LESSONS）都先問「這三段誰做」。工具會折舊、canonical 會漂移，但「大批東西進來時別一把抓」這個切法，比任何單一工具活得久。

🧬

---

_v1.0 | 2026-06-19 13:12 +0800_
_session distill-tooling — 哲宇 directive 把 distill 方法儀器化 + 進化 dna_
_誕生原因：剛手工跑完 266→8，哲宇要求固化方法讓機制長期更精煉_
_核心洞察：把「讀平行 / 寫 deterministic / 判斷自己扛」從這次的做法升成 REFLEXES #72，比工具本身活得久；工具會折舊，切法不會_
_LESSONS-INBOX 候選：無（本 session 是 distill 流程的元層，產出直接 canonical）_
