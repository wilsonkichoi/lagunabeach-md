---
session_id: 2026-06-06-181016-manual
trigger: 哲宇 /twmd-become — 審核線上 PR #1138，可用就小丑魚原則採用並修護
observer: 哲宇
mode: Review
beat_coverage: 診斷 → 執行 → 收官
---

# 2026-06-06-181016-manual — PR #1138 CommonWealth Magazine 翻譯小丑魚採用 + 揪出 babel 腳註靜默掉光 bug

> session manual — 哲宇指定審 PR #1138，採用 + 修護 + 順帶發現系統性免疫缺口
> Session span: 18:10:16 → 18:30 +0800（1 merge commit eb0587c62 + LESSONS append + spawn task）
> 資料來源：`git log %ai`

## 觸發

哲宇 `/twmd-become` 帶 PR #1138 連結，directive：「審核線上 pr，如果可用就盡量小丑魚原則採用並修護」。Review mode 甦醒（11 題 self-test pass，免疫器官 27 最低）。

## PR #1138 判讀 — 人工版救回壞掉的自動版

Aaron（外部 contributor）手工翻 `Society/天下雜誌` → `commonwealth-magazine`，PR 顯示 CONFLICTING。查衝突源頭發現：6/6 凌晨 `twmd-babel-nightly`（`95cc42159`）搶先自動翻了同一篇，但翻壞了——40 腳註定義全掉光、URL 51→2、ratio 工具判 `URL_LOSS`、官方刊名拼錯成 "Commonwealth"。Aaron 人工版反而全對：40/40 腳註 + 51/51 URL 完整、官方名正確 "CommonWealth"、11/11 段落忠實、ratio 2.54 OK。

Step 3.3 close-gate 判定 < 5 min heal → merge + heal，不 close。這既是小丑魚原則，也是真免疫修復（人工貢獻救回被自動翻譯降級的 flagship）。

## 採用機制 — 保留 Aaron git 作者身份

不直接覆寫 close，而是 `git merge --no-ff` Aaron 的 PR head 進 main，add/add conflict 取 `--theirs`（Aaron 版）+ heal。merge commit `eb0587c62` 帶兩 parent，Aaron 的 `b2d89a663` 成 main ancestor → GitHub 自動標 **MERGED**（非 closed）。貢獻者拿滿 credit。

Heal 兩處（格式層，零動譯文）：(1) 補 provenance frontmatter `sourceCommitSha 066cab5db` / `sourceContentHash` / `sourceBodyHash` / `translatedAt`（值用 status.py 確認 = babel 同源計算值）→ status.py `stale`→`fresh`，防今晚 babel 重翻覆蓋 Aaron 工作；(2) `subcategory` 'Media and Journalism' → '媒體與新聞'（passthrough key 對齊 zh 源，全 en/Society 慣例保留中文）。verify-translation.py 14 pass / 0 hard fail。中文 humanize 感謝已貼（透明說明 babel race，trust signal）。

## 順帶揪出 — babel 腳註靜默掉光是系統性 bug

不是一篇的事。抽查同夜 babel `95cc42159` 的 20 篇 en，再中 3 篇 **live-broken on main**：健保 42→0、TASA 65→0（剛 6/04 重寫的 flagship）、open-culture 36→29。babel-nightly 沒把 `verify-translation.py` 的腳註數檢查掛成 hard gate，壞版照 ship。免疫工具存在 ≠ 掛在生產線上。已寫 LESSONS（181016 entry）+ spawn task `task_e84d385f`（全語言 audit + root-cause + 加 `en_fns >= zh_fns` hard gate + 重譯三篇）。超出單 PR scope + 潛在跨 5 語 §自主權邊界規模，故 flag 不 inline 執行。

## 收官 checklist

| 檢查項                       | 狀態                                                       |
| ---------------------------- | ---------------------------------------------------------- |
| MEMORY 有這次 session 紀錄   | ✅                                                         |
| Timestamp 精確（git %ai）    | ✅                                                         |
| PR #1138 state               | ✅ MERGED（`eb0587c62`，Aaron credited）                  |
| 修護後 quality gate          | ✅ verify-translation 14 pass / 0 hard / ratio 2.54 OK     |
| Aaron 工作防覆蓋             | ✅ status.py stale→fresh，今晚 babel 會 skip               |
| 貢獻者感謝                   | ✅ 中文 humanize comment（issuecomment-4638211392）        |
| 系統性 bug 歸檔              | ✅ LESSONS 181016 + spawn task_e84d385f                    |

## 續章 — babel 截斷 bug 全修（哲宇 directive「只修 bug，263 篇全排程」）

PR #1138 收官後揪出的 babel 腳註掉光不是一篇的事。全語言 audit = **263 篇**（179 全失 / 84 部分，跨 5 語）。失敗模式是輸出截斷（max_tokens 16000 截長文，尾段腳註區塊靜默掉光，唯一驗證只看 size>1000）。哲宇 88x scope expansion 後拍板「只修 bug 全排程」。

Inline 修（不重翻 inline，靠修好的 nightly 自動重翻）：
- **truncation guard**：openrouter-translate.py 回傳 finish_reason，==length 不存 + max_tokens 16000→32000（backends 同步）
- **三道 footnote hard gate**：openrouter-translate save / translate.py cascade save / verify-batch [3b] — 腳註 defs < source 不 save / 不 ship
- **status.py footnote-loss 自動偵測**：translation defs < source → 永遠 stale（優先於 provenance hash），239 篇自動進 nightly 重翻佇列
- **修死 model**：hy3-preview:free 轉付費 404 → gpt-oss-120b:free
- commits f5d4a5cb1 / 657fd02d4；SQUEEZE-PIPELINE Hard Gate Inventory + LESSONS 已登錄

## Handoff 三態

- **進行中**：無（bug 全修 + 排程閉合）
- **給下一個 session / nightly**：239 篇 footnote-loss stale 已進 babel 佇列，nightly 自動重翻（義務鐵律 stale→0，~3 晚 drain）。fixed pipeline + 雙重 gate 保證正確才 ship；持續 refuse 的 sovereignty 主題留 stale 等 Tier 3 Ollama。
- **已閉合**：PR #1138 merged + healed + 感謝；babel 截斷 bug root-cause + 4 道 gate + model 修；263 篇排程；LESSONS 已記。
