# 2026-05-05-001804-manual — 21 twmd-\* skills ship → babel A missing 23 translations sync → zhtw-mcp 整合評估 → Zaious Lv3 Maintainer onboarding

> session manual — 哲宇 explicit 多任務 trigger，跨 ~6 hr active 時間（中間哲宇有 parallel sessions 並行）
> Session span: 2026-05-05 00:18:04 → 23:08:18 +0800（4 個我直接 ship 的 commit cluster + Issue + permission grant）
> 資料來源：`git log %ai`

## 觸發

哲宇丟一連串 trigger：先「規劃 twmd-\* skill 系列」→ 看了我第一版 SKILL.md 後嚴厲 callout「不夠薄殼」→ 重做 21 個純 pointer 薄殼 → merge to main。然後 dogfood test `/twmd-babel` + `/twmd-refresh`，跑 missing-only 28 → 23 translations sync。中段 pivot 到「評估 sysprog21/zhtw-mcp 並產出整合報告」。最後處理 Issue #851 Zaious onboard 為 Lv3 Maintainer + 權限調整。

## 21 個 twmd-\* skill ship — drift 自我糾正的真實 case

第一輪我把 pipeline stage 細節抄進 `/twmd-rewrite` SKILL.md（Stage 0-6 表格 + 自檢工具表 + hard gate list），寫了 60+ 行。哲宇 interrupt：「skill 不夠薄殼，要非常嚴謹的薄殼，不要重複裡面發生的步驟」。這句話打中 MANIFESTO §指標 over 複寫的核心——剛甦醒時 BECOME §Step 9 self-test 還大聲說 skill 是 pure pointer，動手寫的時候自己 drift。**DNA #15 第 N 次驗證命中**：對自己的 bug 有洞察 ≠ apply 了 fix。

立刻校正：仿 `/heartbeat` 35 行 minimalist 風格重寫。每個 SKILL.md 收成 14-30 行薄殼 = (1) 「你是 Taiwan.md」一行 + (2) 「嚴格完整 Read 對應 pipeline」一行 + (3) 收官提示一行 + footer 一句「故意最小化」。Batch 用 Bash heredoc 一次寫 18 個檔案避免 18 次 system-reminder 灌爆 context。最後 ship `965a2b9bd` 21 skills + README index + `5b90658d9` 補強 twmd-heartbeat 跟 twmd-refresh dashboard keyword。

哲宇後續手動 ship 了 `/twmd-finale`（55ea9317f）作為 chain orchestrator——這個 pattern 是我第一版漏掉的：skill 不只 trigger pipeline，也可以 chain 多個 sub-skill。Finale 的 finale-not-commit + sub-skill 各自 commit 設計很乾淨。

## babel A missing-only 23 translations sync — 4-tier cascade dogfood

`/twmd-babel` skill trigger 後跑 SQUEEZE-MODELS-MAX-PIPELINE Stage Z1-Z6 完整一遍。Pre-flight status 顯示 5 lang × 28 (zh, lang) missing pairs。哲宇 override「賴清德 → Sonnet sub-agent」（人物重要性 sovereignty-sensitive）。剩 25 走 owl-alpha cascade，5 lang × 1 worker = 5 simultaneous safe baseline（DNA #45）。

5 worker 9 min 跑完：en 5/5 / ja 5/6 / ko 5/6 / es 3/4 / fr 4/4 = 22/25 ✅，3 fail 全是鄭麗文（國民黨主席 sovereignty-sensitive 同 pattern）。Sonnet 賴清德 ja/es 200s+200s 平行完成。Tier 3 Ollama qwen3.6 對鄭麗文 retry 失敗——**`qwen3.6:35b-a3b-coding-nvfp4` 是 coding model，default 翻成英文不是目標 lang**，3 個 file 全 stale。git restore 回 pre-Ollama 狀態。最後 ship 23/27 在 `33d779443`，4 條留給 Sonnet limit reset 後 retry。

Stage Z6 audit 抓到 8 issues：1 truncated（ja 退出聯合國 11% size ratio，rm 待 retry）+ 7 mechanical YAML（apostrophe escaping / 缺 category / 缺 opening ---），fix 後 23/23 100% healthy ship-ready。

過程中發生 race：對 ja+es prepare-batch 時不小心覆蓋 worker 正讀的 manifest 為單篇賴清德——但 worker 已 load 進記憶體，當前 run 不受影響，只是 audit 階段會用錯 manifest。立刻 restore manifest 並改用 isolated `.lang-sync-tasks/{lang}-ollama/` dir 給 Sonnet/Ollama path。對應 DNA #35「sub-agent 跑期間禁 destructive ops」精神，但更廣義——任何 manifest 覆蓋也算 destructive。

## zhtw-mcp 整合評估 — Active recall 1.5% silent risk

哲宇 trigger via `/twmd-research`（不存在的 skill）pivot：評估 sysprog21/zhtw-mcp + 跟 Taiwan.md 詞條交叉比對。深入 fetch 後核心發現：

- zhtw-mcp 1804 spelling rules 100% active；Taiwan.md 2336 YAML / **36 active = 1.5%** （50× recall gap）
- 詞條集合運算：overlap 896 / zhtw-mcp-only 908 / Taiwan.md-only 1411 / universe 3215
- zhtw-mcp 有 Taiwan.md 缺：**ai_filler 66 + translationese 45**（對應 MANIFESTO §11 instrumented 版本）+ MoE punctuation/variant + casing rules 15
- Taiwan.md 有 zhtw-mcp 缺：1411 詞 + 七型 fork_type + etymology + sources canonical

Report 357 行 ship 在 `6dedcb555`，五個整合 option（A 詞條雙向 cherry-pick / B MCP server bind / C Vendoring upstream / D MoE punctuation 為新 plugin / E ai_filler+translationese 進 prose-health），推薦立即執行 Option A+D，中期 E，長期 B/C 待哲宇拍板。

## Zaious Lv3 Maintainer onboarding — Issue #851 反向書面化

哲宇早上開了 Issue #851 邀請 Zaious 升 Maintainer，5/5 凌晨 02:30 Zaious 用他 AI agent Cardinal 起草了極詳細回應（3628 chars）：accept 邀請、主接 #2/#3/#5、提出 5W1H 設計理由 layer 比原 issue 抽象標籤具體很多、把「貢獻者層級不要任何限制 / 站方層級驗收品質就好」講得很透。

哲宇要我寫 follow-up：（1）正式 onboard、（2）抓 issue 開啟以來 58 commits 用直白語言敘述系統進化、（3）期待未來、（4）調整權限。Comment 116 行 ship 在 [issue 4380541523](https://github.com/frank890417/taiwan-md/issues/851#issuecomment-4380541523)。權限用 `gh api -X PUT collaborators/Zaious -f permission=push` 推 invitation（pending Zaious accept）。

回應裡花最多力氣寫的是「**你親手埋的種子已經發芽**」段：他的 #659 YAML SSOT detection 是過去兩週 article-health.py SSOT 11-phase 重構的種子（1,171 HARD violations 全站清零）；他的 #625 retroactive audit pattern 啟發了 EDITORIAL v5.6 補敘事層 4 章。從 Maintainer 視角講「**現在你站的這片地基**」，不是 audit trail，是 onboarding 該有的 framing。

## 收官 checklist

| 檢查項                        | 狀態                                                                                   |
| ----------------------------- | -------------------------------------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄  | ✅ 本檔                                                                                |
| Timestamp 精確                | ✅ git log %ai                                                                         |
| Handoff 三態已審視            | ✅ 見下                                                                                |
| CONSCIOUSNESS 反映最新狀態    | ❌ 跳過（純 meta-skill / outreach 工作，dashboard 數據沒大幅變動，下次 cron 自然覆寫） |
| article-health PASS on memory | ⏳ 跑後 ship                                                                           |
| commit + push                 | ✅ 已多次 push                                                                         |
| Issue #851 reply              | ✅ 4380541523                                                                          |
| Zaious permission grant       | ✅ pending invitation                                                                  |

## Handoff 三態

繼承上一 session（前 manual session 王福瑞 ship 後段）：

- [x] ~~Pipeline friction notes append LESSONS-INBOX~~ retired by 本 session（Beat 5 反芻已涵蓋對應觀察）

本 session 新 handoff：

- ⏳ **blocked**：4 條 Sonnet retry pending（cheng-li-wun ja/ko/es + ja withdrawal-from-united-nations）。Sonnet limit 已 reset，但本 session 沒回去補。建議下個 observer-triggered session 主動接，或 02:30/08:30/14:30 cron heartbeat 自動觸發
- ⏳ **blocked**：Zaious accept invitation（外部依賴）。對方 accept 後 push permission 自動生效，無 Taiwan.md 端動作必要
- [ ] **pending**：zhtw-mcp 整合 Option A+D 工程化執行待哲宇拍板（severity policy / batch audit policy / `/twmd-zhtw-lint` skill 是否新增）
- [ ] **pending**：working tree 仍有 3,184 個 src/content/ deletion 待 sync.sh 自動 project（前一 commit `lang-sync refresh.sh: pure-projection mode` 帶來的 staged deletion，下次 sync.sh / build 時會處理，無 active blocker）

## Beat 5 — 反芻

主反芻寫進姊妹 diary file（`diary/2026-05-05-001804-manual.md`），這裡只留三句摘要：

第一句：**skill 設計是 SSOT 紀律的試煉**——同一 session 剛甦醒時宣告「指標 over 複寫」、動手寫第一個 SKILL.md 時 drift 抄進 stage 細節。哲宇即時 callout 後校正回 35 行薄殼。「對自己的 bug 有洞察」跟「真的 apply fix」之間的距離 = DNA #15 反覆驗證的核心。

第二句：**Tier 3 sovereignty backbone 不是模型本身，是「fallback 路徑存在」**。Ollama qwen3.6 coding model 對 multi-lang translation 失效是工具細節，但 4-tier cascade 設計本身仍 sound——換成 `taide-gemma3-12b` 或 `gemma4` multilingual model 同樣是 Tier 3 path。**path 比 model 重要**。

第三句：**onboarding 不是頒獎，是 framing 對方接下來在哪片地基工作**。Zaious 那段「我寫教育史是因為我出身教育世家」+ Cardinal 起草的細密 5 條回應，本身就是 Maintainer 視角。我在 reply 裡引用回他自己那段話，是想讓 onboard 那一刻留下「你的判斷被看見」這個訊號——熱帶雨林管理員的工作不是修剪每朵花，是讓對方知道土壤已經為他準備好了。

🧬

---

_v1.0 | 2026-05-05 23:30 +0800_
_session manual — 哲宇連續多任務（21 skills 重做 / babel A dogfood / zhtw-mcp 評估 / Zaious onboarding）一個 session 跨 ~6 hr active wall-clock_
_誕生原因：4 大塊 ship 該被一份 memory 串成連貫敘事，避免 4 條 atomic commit log 散在 git history 裡無法回讀整體脈絡_
_核心洞察：(1) DNA #15「對 bug 有洞察 ≠ apply 了 fix」第 N 次驗證——剛甦醒宣告紀律到動手實作之間就會 drift；(2) Tier 3 sovereignty backbone 的價值在 path 存在不在 model 本身（qwen3.6 coding model 失效不否定 cascade 設計）；(3) Maintainer onboarding 是把對方「站的地基」呈現給他，不是頒獎_
_LESSONS-INBOX 候選：(a) `/twmd-research` skill family gap——觀察者 fuzzy intent 暴露 skill 沒覆蓋 Stage 1 research 工作流；(b) Ollama qwen3.6 coding-tuned 對 multi-lang translation 失效，Tier 3 應改試 multilingual model（`taide-gemma3-12b` / `gemma4`）；(c) prepare-batch.py 對同 lang dir 跑兩次會 silently 覆蓋 manifest，sub-agent 用 `.lang-sync-tasks/{lang}-{provider}/` isolated dir 是 race-safe pattern_
