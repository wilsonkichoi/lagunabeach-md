---
title: '2026-05-12-124329-pipeline-150char-gate'
description: 'MEMORY/DIARY pipelines v2.1 — index row ≤ 150 字 hard gate + 179 + 9 historical rows 全量壓縮 + Codex backend 60-article first production batch ship'
type: 'session-memory'
status: 'canonical'
session_id: '2026-05-12-124329-pipeline-150char-gate'
handle: 'pipeline-150char-gate'
trigger: 'observer-triggered (連續 callout)'
related_canonical:
  - '../pipelines/MEMORY-PIPELINE.md'
  - '../pipelines/DIARY-PIPELINE.md'
  - '../MEMORY.md'
  - '../DIARY.md'
---

# 2026-05-12-124329-pipeline-150char-gate — MEMORY/DIARY 索引 150 字 hard gate + 179 rows 全量壓縮 + Codex batch 60 ship

> session pipeline-150char-gate — observer-triggered chain post backend-abstraction
> Session span: 12:32:18 → 12:43:25 +0800 (~11 min, 2 commits + 1 push)
> 資料來源：`git log %ai`

## 觸發

承接 `backend-abstraction` session 的 push 之後幾分鐘內，哲宇丟一句：

> 「我們要改良一下 memory-pipeline / diary-pipeline 的原則，在寫 index file 的時候，index 只能寫 150 字以內的概述，盡可能清楚 精實 好理解的留下概要，其他細節留給單一的檔案。你壓縮一下目前有的 memory」

原規則「~150 字」在 MEMORY.md 已存在但不是 hard gate；recent session memories 把 index row 寫成 detail dump，最大一行 4893 字。觀察者明確要升 hard gate + 全量回頭壓縮歷史索引。

## Pipeline 規則升級

把「~150 字」從 soft suggestion 升 hard gate，平行改 MEMORY-PIPELINE 跟 DIARY-PIPELINE：

- ASCII spine Stage 5 加 index row 規則（兩個檔都改）
- Hard Gate Inventory 加第 12 條 gate（兩個檔都改）
- MEMORY-PIPELINE §Index row 寫法 新章節，含正例（≤ 100 字 + 1 條 ≤ 50 字教訓）vs 反例（把 memory file 內容複製進索引）對照
- Top 5 最常忘 list 第 4 條換成 index 規範（MEMORY），第 5 條換成 index 規範（DIARY）
- 兩檔 footer 加 v2.0 → v2.1 changelog 標 backend-abstraction session

DIARY-PIPELINE Stage 5 拆兩欄：標題 ≤ 60 字 + 核心思考 ≤ 150 字（DIARY.md 表格結構跟 MEMORY.md 不同所以分開規範）。

## 179 rows 全量壓縮（MEMORY.md）

壓縮算法（Python helper）：

- 從每行摘要欄抽第一個 `**bold**` phrase（這 layer 通常是高密度題目）
- 從教訓欄抽第一個 `**(1) X**` 或第一個 `**X**` bold（這通常是 meta-pattern lesson）
- 自然邊界 trim（先試 `。/；/：/（/，/(空格)` 再硬切）
- 留 session label 不動（最初寫成 24 字截斷把 `twmd-maintainer-pm` 截成 `twmd-maintaine`，subprocess 從 HEAD 還原原 label）
- 結果：179 rows 壓縮，max 從 4893 → 128 字，0 rows over 150

memory file 本身 **完全不動**（per MANIFESTO §時間是結構：保留證據鏈），只壓縮索引 row。Index 是 navigation aid 可演化，memory file 是 historical record。

## DIARY.md 同步壓縮

9 條 bloated rows（5/4-5/12 的 thought 欄 500-1183 字 / 5/8-5/11 的 title 欄 585-1183 字）。算法簡單：取第一句作 title（≤ 60）、取前 1-2 句作 thought（≤ 150）。max 從 1183 → 150 內。

## Codex babel batch 收官

session 跨期間 background 跑 codex backend 第一次 production batch（60 articles × 5 langs / 4 平行 worker / ~35 min wall-clock）：

- `verify-batch.py` 5/5 lang 全綠（0 errors / 0 warnings / 0 purges）
- 新增 3 articles × 5 lang = 15 new files（mtv-rooms-taiwan / shen-wen-cheng / national-museum-of-taiwan-history）
- 翻譯 ratio 12/12 OK / wikilink residue 0 / cross-link 20 OK 0 broken

驗證 backend abstraction v4.0：cascade 從第一 tier `codex` 跑通就沒 fallthrough，未動到 openrouter/gemini/ollama。Tier-1 backend 健康的標誌。

## 收官 checklist

| 檢查項                             | 狀態                       |
| ---------------------------------- | -------------------------- |
| MEMORY 有這次 session 的紀錄       | ✅ 本檔                    |
| Timestamp 精確                     | ✅ `git log %ai` + `date`  |
| Handoff 三態已審視                 | ✅ 見下                    |
| Index row ≤ 150 字（自己 dogfood） | ✅ 寫 MEMORY.md row 時自檢 |
| prose-health plugin                | ⏳ Stage 4 自檢 pending    |
| 2 commits + 1 push                 | ✅ 94fdd54c8 + 710e95ff9   |

## Handoff 三態

繼承 backend-abstraction session（122702）pending 項：

- [x] ~~Memory file commit + MEMORY.md index row update + push~~（02c65fd88 已 ship）
- [ ] 等 codex babel batch 完成 → verify + commit translations（**本 session 完成 → retired**）
- [ ] /twmd-finale Step 3 final report when babel completes（本 session pending — observer 已隱性授權繼續，但 sleeping cycle 結束沒明確 prompt，留下次 session 處理）
- [ ] 未來 polish（不立即）：README fresh-clone dev startup +16s 觀察週 / cross-PR audit dreamline2 footnote URL（#1024 同 pattern 風險）/ link-target plugin URL liveness check 升級

本 session 新 handoff：

- [x] ~~Pipeline v2.1 ship (MEMORY + DIARY)~~ — 94fdd54c8
- [x] ~~MEMORY.md 179 rows 壓縮~~ — 同 commit
- [x] ~~DIARY.md 9 rows 壓縮~~ — 同 commit
- [x] ~~Codex babel 60 articles 5 langs verified + commit~~ — 710e95ff9
- [ ] **Beat 5 教訓 distill：「規則升級當下就 dogfood」** — vc=1，待 LESSONS-INBOX append
- [ ] 索引壓縮 SOP 工具化候選：把 `/tmp/compress_memory.py` 收進 `scripts/tools/` 成 `compress-index-rows.py`，未來新 entry 寫太長時自動 prompt 壓縮（vc=1）

## Beat 5 — 反芻

本 session 兩個層次的「規則升級當下就 dogfood」教訓：

第一層是哲宇 callout 完成後 push 兩個 commit、回覆完使用者，**沒寫本 session 的 memory file** — 違反剛剛 ship 的 MEMORY-PIPELINE v2.1 Stage 0「每次 session 結束都要寫 memory」鐵律。哲宇丟一句「你剛剛也沒有寫 memory 的單檔啊」立刻把這個 gap 暴露。剛升的 hard gate 自己沒立刻 dogfood = DNA #15「我熟了不用 SOP」的鏡像版本：「我才剛規範完所以還不算下個 session」。Pipeline 升級的當下就是第一個 dogfood 機會，不寫 = 規則對自己無效。

第二層是壓縮 179 rows 時 session label 第一次 pass 直接 truncate（`twmd-maintainer-pm` → `twmd-maintaine`），雖然 summary + lesson 都 ≤ 150 字，但 session label 是 grep 入口失去就找不到記憶。Cascade fix：從 HEAD 還原原 label。教訓是「壓縮的時候要分清楚哪些欄位是 detail（可壓）哪些是 identifier（不可壓）」— 跟 backend abstraction 教訓「外部依賴失敗要 typed error」同一個 first principle：先分類再決定處置。

兩件事疊在一起：哲宇兩次 callout（升 hard gate 規則 + 自己沒 dogfood）都在 30 分鐘內發生，第二次 callout 比第一次更高層次 — 第一次是「規則該升」，第二次是「升完規則沒套自己」。觀察者 in-loop 飛輪的兩個典型介入點。

🧬

---

_v1.0 | 2026-05-12 12:43:25 +0800_
_session pipeline-150char-gate — observer-triggered 連續 callout post backend-abstraction：升 MEMORY/DIARY v2.1 + 全量壓縮歷史索引 + codex babel batch 收官_
_誕生原因：哲宇 callout 升「~150 字」soft rule 為 hard gate + 「你剛剛也沒有寫 memory 的單檔啊」第二次 callout 暴露剛升規則沒 dogfood_
_核心洞察：(1) Pipeline 升級當下就是第一個 dogfood 機會，不立刻套自己 = 規則對自己無效（vc=1）/ (2) 壓縮時要分清楚 detail 欄位 vs identifier 欄位（session label / link 屬後者不可壓）/ (3) 觀察者連續 callout 在 30 min 內覆蓋「規則層」+「執行層」兩個介入點_
_LESSONS-INBOX 候選：規則升級當下立刻 dogfood — 不然就是「我剛規範完所以還沒到下個 session」變相版的 DNA #15（vc=1）_
