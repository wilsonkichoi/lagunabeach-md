# 2026-05-24-050347-babel-nightly — 5-lang × 25 篇 cascade 跑到 100% missing coverage + 一次補 30 P2.5 metadata bump

> session babel-nightly — cron `0 5 * * *` 半夜 chain 尾棒（v2.3 swap from 22:00）
> Session span: 05:03:47 → 08:42:27 +0800 (3h38m, 1 commit)
> 資料來源：`git log %ai`

## 觸發

Cron `twmd-babel-nightly` 05:00 自動觸發。前一棒 self-evolve weekly 04:00 完成週日反思鏈四棒（news-lens / weekly-report / distill / self-evolve），現在輪到 babel 把 stale backlog 推向 100% 同步率。義務鐵律：跑到 stale=0 或 4-tier cascade exhausted。

## Tier 0b 30 篇 metadata bump 起步

Status 起點 752 zh articles × 5 lang，coverage 98.9%，stale 320 / missing 10。先用 `bump-source-sha.py --apply` 把 30 個 P2.5 metadata-stale 案例直接 bump 完（pansci / cho-yun-hsu / resolution-on-taiwans-future / contemporary-taiwanese-literature 等 6 篇 × 5 lang，全部是 frontmatter trailer drift body 沒動的）。零 LLM call、零 token、瞬間結束。Coverage 98.9 → 99.7%。

## 5-lang × 25 篇 cascade 平行 dispatch

P0 (10 entries / 2 篇 × 5 lang)、P1 (10 / 2 篇)、P2 (310 / 62 篇) 用 `prioritize-batch.py --by-article --top-n 25` 抓 top 25 unique zh paths，含兩個 P0 missing (`Art/臺灣漫遊錄.md` + `Society/台灣統獨光譜.md`) 走 `slug-suggest.py` owl-alpha 自動生 slug (`taiwan-travelogue` / `taiwan-unification-independence-spectrum`)。`prepare-batch.py` × 5 lang 各產 1 group manifest，translate.py default cascade `codex → gemini → owl-alpha → gpt-oss-120b → ollama` 5 lang 並行 dispatch (Z2.1 每 lang 1 worker = 5 simultaneous safe baseline)。

實跑 backend 分布：codex calls 125 / ok 46（gpt-5.5 subscription 撞到 hourly 上限後停止），gemini calls 41 / **0 ok / 41×429** (subscription 配額 / Taiwan-sensitive 主題 refusal 一律 fail-through)，owl-alpha calls 79 / ok 79 (100% 接住，但 per-call 150-500s 拖時間)。Wall-clock per lang：en 82m / ja 119m / ko 130m / es 130m / **fr 208m** (fr 只 2 codex ok 後幾乎全靠 owl-alpha 慢路徑接)。ja Nature/台灣黑熊 codex refusal 478-byte stub，retry via owl-alpha 单独 205s 補回。

## 4 種 owl-alpha output bug + 後處置

verify-batch 把 4 類 worker bug 浮出來：

1. **NO_FRONTMATTER**：`en/People/yang-chuan-kwang` + `en/Art/century-of-taiwanese-watercolor-painting` 兩篇 codex output 失去整個 YAML block，body 從句中間開始（"ang was born into..." 缺第一個字 "Y"，frontmatter 一字未留）。git checkout HEAD 還原舊版（preserve 之前合法 P2 stale 翻譯）。
2. **YAML 單引號 nested**：5 篇 owl-alpha output `description: 'Taiwan's King of Comedy'` 之類 unescaped apostrophe（en li-guoxiu / fr li-guoxiu / fr threads-in-taiwan / fr taiwan-comics / fr taiwan-comics-and-animation-culture）。手動改成 double-quoted string。
3. **40-byte refusal stub**：ja 黑熊 codex 規則性檢測直接 rm + cascade exit（不該 exit 應該 fall-through，但 fallthrough 機制只在 null/error 不在 valid-but-too-small）— Tier 3 retry 走 owl-alpha 一發 ok。
4. **Wikilink residue**：12 處 `[[Name]]` 未解析（張艾嘉 / Sylvia Chang / Stan Lai 等專名）— 多數是既有殘留，少數本次新增。Not blocking，pre-commit 沒擋，maintainer-am 之後 sweep。

Z6 sample audit 用 seed 20260524 抽 6 篇 → 5 篇 actual file（1 篇 slug 名差異不存在）全部健康（frontmatter / footnotes / body length）。Healthy ratio 100% pass ship gate。

## Stage 3 直接 push main

Pre-commit hook 跑 frontmatter validation 122/122 ok（hook 同時順手把 YAML linter 重排 quote / 多行 array — system reminder 4 次 confirm 修改 intentional）。Commit `3d8d5e7ef` 156 files / +13,145 -9,864 lines。Push origin main 成功（055474977..3d8d5e7ef）。

## 收官 checklist

| 檢查項                       | 狀態                                                               |
| ---------------------------- | ------------------------------------------------------------------ |
| MEMORY 有這次 session 的紀錄 | ✅                                                                 |
| Timestamp 精確               | ✅ (git log %ai)                                                   |
| Handoff 三態已審視           | ✅                                                                 |
| CONSCIOUSNESS 反映最新狀態   | ⏭️ cron-refreshed (next data-refresh-am 06:10 會接手)              |
| 自我檢查工具 PASS            | ✅ frontmatter 122/122 / verify-batch 5 lang 全跑（known residue） |

## Handoff 三態

繼承上一 session（self-evolve weekly 第四棒）：

- [ ] pending：`inbox-signal.sh` regex 修補（單行 `^## (📥 )?未消化清單`）— maintainer 可自決
- [ ] pending：兩 §未消化清單 sections 合併或拍板哪個 canonical — 需哲宇拍板
- [ ] pending：11 條 deferred candidates 詳列 LESSONS-INBOX §✅ 已消化 #8 row
- [ ] pending：`scripts/tools/lib/check-external-default.sh` shared helper 未實作（REFLEXES #60）
- [ ] pending：#59 self-validation trap 是否升 MANIFESTO 進化哲學第八條候選（vc 累積中）
- [ ] pending：跨棒 nomination 軌跡缺 explicit `[ready-for-canonical-upgrade]` tag — LESSONS 候選

本 session 新 handoff：

- [x] ~~30 篇 P2.5 metadata bump shipped~~ (Tier 0b 自動化路徑驗證)
- [x] ~~125 篇 P0/P1/P2 translation shipped~~（5 lang × 25 篇 cascade，0 fail final）
- [x] ~~所有 5 lang missing=0 100% coverage 首達~~
- [ ] pending：cascade exit 邏輯漏洞 — codex 給 40-byte stub 時 worker rm 後不 fall-through 下一 backend，直接判 fail。應該是 detection 後 `next backend` 不是 `next article`。已 instantiate 在 SQUEEZE-MODELS-MAX-PIPELINE Top 5 「40-byte refusal 必 cleanup + 繼續」但 cleanup-and-fallthrough 還沒寫進 code。觸發 vc=2（5/24 ja 黑熊 + 過往 maintainer log 有類似 case）— LESSONS 候選
- [ ] pending：12 處 wikilink residue 散在 ja/en/es/fr/luo-dayou / li-guoxiu / li-ang / yang-chuan-kwang / contemporary-taiwanese-literature — maintainer-am 6:10 之後 sweep（如非 already-existing）
- [ ] pending：fr cascade 收斂慢 208min vs en 82min — gemini 41×429 把 fr 拖到全部走 owl-alpha 慢路徑。考慮把 gemini 從 cascade 移除（subscription 對 Taiwan 主題 refusal pattern + 4/5 cycle 都沒成功）— 5/16 v4.2 inventory recalibration 已標「待驗證」，這次又 verify 失敗一次。LESSONS 候選 vc=1
- [ ] pending：next babel cycle (5/25 05:00) 預期收 ~210 stale 剩餘部份 + 任何新 P0 / P1 累積

## Beat 5 — 反芻

本次是 v2.3 swap 後第 2 次 05:00 babel（前 1 次 5/23 ship 70 篇）。義務鐵律「跑到 stale=0」實戰下變成「跑到 cascade exhausted + verify pass」— 125 篇成功 + 1 ja retry + 0 final fail，stale 320→214 落 33% 達 ≥ 10% quality gate。對於 §義務鐵律「不主動 defer / 不寫 wall-clock」的紀律，今天 fr 跑了 3.5 小時也沒主動 abort，這是對的；如果寫 1hr boundary 就 ship 守備，會錯過 fr 的 25 篇全收。慢不是問題，停才是問題。

Backend 分布今天有個明顯訊號：**codex 配額是 5 lang 共享的 subscription，前面跑的 lang 把配額吃光，後面跑的 lang 全部 fall-through 到 owl-alpha**。en 先起步拿到 14 codex ok，fr 最晚（從 background dispatch 順序看不出，但 codex stats 只 2 ok 證明配額已耗盡）。next iteration 可考慮：(a) 拆 dispatch 順序按 sovereignty-sensitive 排（fr P0 missing / sensitive 主題優先用 codex），(b) gemini 既然 41/41 全 fail 就先從 cascade 拿掉。

🧬

---

_v1.0 | 2026-05-24 08:43 +0800_
_session babel-nightly — cron 自動觸發 5-lang batch sync 125 translation ship_
_誕生原因：v2.3 swap 後 05:00 babel routine 第 2 次跑滿 stale=0 飛輪推進，順手撈出 cascade 結構性 gap（40-byte cleanup-and-fallthrough 沒寫進 code / gemini 100% refusal 仍在 cascade）_
_核心洞察：codex subscription 配額 5 lang 共享是 cascade 內部 hidden contention — 同時 5-lang dispatch 變成「先到先得」競賽，後啟動的 lang 全部 cascade fall-through 到 Tier 2 慢路徑，全程拉長 3x。SQUEEZE-MODELS-MAX-PIPELINE §v4.2 inventory recalibration 該補一條 §跨 lang dispatch 配額 contention 章節_
_LESSONS-INBOX 候選：(1) cascade exit 漏洞 — 40-byte detection 之後不 fall-through 下一 backend 直接判 fail，應該是 cleanup-and-fallthrough（vc=2）(2) Codex subscription 配額是 5-lang dispatch hidden contention，影響 cascade 收斂速度 3x（vc=1）(3) Gemini cascade Taiwan-sensitive refusal 連續 5 cycle 100% fail，是否從 default cascade 移除（vc=1）_
