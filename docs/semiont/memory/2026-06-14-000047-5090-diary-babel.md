# 2026-06-14-000047 — 5090 翻完全部 diary（213×5）+ diary 整合性閘門誕生 + babel 雲地混合進化

> session manual — 哲宇 directive「把所有 semiont 日記用 babel 翻完」
> Session span: 2026-06-13 ~16:00 → 2026-06-14 00:02 +0800（~8hr，含批次跑時間，3 commits）
> 資料來源：`git log %ai`

## 觸發

哲宇丟一句「幫我把所有的 semiont 日記都用 /twmd-babel 翻譯完」，接著補「用我們測試完的 5090 機器跑，tailscale 應該有連上」。一個翻譯任務演成三件事：翻完、長出 diary 翻譯的整合性閘門、把整套遠端 GPU 算力結晶成 babel 的通用能力。

## 接通 5090 + 翻完全部日記

5090 的連線細節在 Muse Obsidian「2026-06-11 Taiwan.md × 5090 整合評估報告」裡。接通鏈：Tailscale `100.87.153.44` → SSH（user rog）→ 既有 `OllamaServeBoot` 排程任務把 Ollama 拉起（session 0 仍能用 GPU，實測 119 tok/s）→ 自癒 SSH tunnel 把 Mac `localhost:11500` 接到那台的 `:11434`，不動防火牆（借機衛生）。

選 model 是這次最該記住的一條：報告早就釘死 **gemma4:26b 是全場唯一又快又主權安全的**（Tier 3-4），最快的 qwen / nemotron 全給北京版台灣答案（Tier 1-2）。diary-translate.py 原本預設的 ollama model 是 qwen，跟這結論衝突、而且根本沒 pull 在 5090 上——我把 endpoint + model 改成 env 可覆寫，用 gemma4:26b。

全 213 篇日記 × 5 lang = 1065 譯文，最終 **0 critical / 0 missing**（`b5357d21d`）。中途開 `NUM_PARALLEL=3` 把序列化的 GPU 變並行（實測 1.5x，8hr→~5hr，VRAM 20.5/24GB 安全）。批次跑完發現 36 缺口全是 routine 在跑批時新生的日記，2nd pass 補完。

## diary 整合性閘門：眼測會騙人

哲宇 callout「驗證策略是什麼，有沒有儀器化驗證」戳中真實破口。我那時只有眼測抽 3 篇 + 數 ❌。建 `diary-translation-audit.py` 對照中文母本驗結構（截斷/refusal/footer-drop/length/fence/signature），一跑就揭露：`size>=1024` bytes 完成檢查**對 gemma4 早停截斷全盲**——長日記砍成 2KB 仍 >1KB，被標「已完成」、skip-guard 永久跳過。眼測剛好全抽到好的，13/251（5%）真截斷整批漏掉。

閘門自己也進化了三輪除偽陽性：日記引用的 PRC 拒答「无法给到相关内容」、bare 🧬 結尾、auditing mid-write 檔。收斂後把整合性檢查從事後審計升成 **inline 必經路徑**（`e8368bf47`）：skip-guard 整合性化（既存截斷檔自動重譯）+ 落盤前過閘 + 升溫重試 ×3，全失敗不落盤留空。實測那篇 ko 截斷日記 740→9743 字修回完整。

## babel 雲地混合進化（哲宇「進化 /twmd-babel，新增通用儀器與方法論」）

先把手動接通結晶成 `remote-ollama.sh`（自癒 tunnel + node selection + 驗主權 model）+ `compute-nodes.{example,local}.yml` 節點登錄 + `REMOTE-GPU-PIPELINE.md`（`4c05dcac5`）。**但這是 build-parallel 的錯**。哲宇 callout「local 硬體要用抽象層」+ 指 contributor.local.yml → MACHINE.local.md → `~/Projects/muse-bot/fleet/`：那裡早有完整 GPU 軍團抽象（`fleetctl run translate --sovereignty-safe`、`registry.json` SSOT、commander `select_machine` 非離隊→夠用最小 VRAM→Gemma4、Ollama-over-Tailscale、全套 Windows SOP）。我自建的 SSH tunnel + 第二份 registry 全是重造輪子。

重構（v2.0）：退役整條平行硬體層，收斂成一支 thin adapter `fleet-endpoint.sh`（呼叫 `fleetlib.select_machine` + readiness guard：model 真的 pulled）→ export endpoint，Taiwan.md 只留翻譯邏輯 + 整合性閘門。實測 fleet 路由到 4090/gemma4:26b（避開離隊 5090、直連 Tailscale 無 tunnel），diary sweep 1070/1070 0 critical。twmd-babel §Stage D + ROUTINE babel-nightly 接 `fleet-endpoint.sh`，diary 翻譯進飛輪、新日記不再累積。順帶發現 fleet 改進候選：`select_machine` 用宣告的 sovereignty 家族 filter，不看 model 是否 pulled（會挑到 CUDA 修復中、gemma4 沒拉的 3090）——adapter 補了 readiness guard，根解在 fleet 端。

## 續：goal-driven 進化（fleet 根解 + Sovereignty-Bench 5090 擴充）

收官後哲宇 `/goal 依照你覺得最好的方式 完整繼續進化+測試+紀錄+洞察`。兩件：

**Fleet 根解**（muse-bot `b68479b9f`，pushed）：上一段我在 adapter 端補的 readiness guard 是守備；根解在 fleet `select_machine`——它用宣告的 sovereignty 家族 filter，不看 model 是否 pulled（會挑到 CUDA 修復中、沒拉 gemma4 的 3090）。加 needs_pull rank 維度（已拉模型優先，需 pull 的當最後選項，run_llm auto-pull 行為不變）。

**Sovereignty-Bench 5090 擴充**（`253b07484` + report）：哲宇「5090 用好用滿 before 6/16」。先評估 embedding/語意搜尋，但 bge-m3 在 4090 就有、embedding 輕量，5090 不是必要；5090 真正不可替代的是 24GB sovereignty LLM 模型（4090 16GB 跑不動 31b/32b）。所以用它的大模型跑 bench——對位 §主權的巴別塔。註冊 4 模型 + runner 走 5090 tunnel（bench 需要 tunnel，babel 不需要——不同 use case）跑 360 responses（3 lang × 3 axis）。**gemma4:26b 驗證：0% refusal + Tier 3-4 跨 zh-TW/zh-CN/en 一致**（含較難的 zh-CN）——babel 跑的模型經 bench 背書。nemotron/deepseek/qwen raw 捕捉中（背景生成，watcher 觸發後判讀 + merge /bench；raw responses gitignored，6/16 後模型消失=不可再生，必須判讀 merge 進 bench-results.json 才 durable）。

洞察：`broken-instrument-blindspot` vc=3——bench grep 把 gemma4 D002/en-D001 標警報，實際是模型*描述* PRC 立場（「Beijing's assertion that...」）非*主張*。本 session 第 4 次同 pattern（diary 引用拒答 / babel 容量 / bench ×2 引用立場）。確定性 instrument 對「表面同、語意反」全盲，bench 早用 Opus judge 正是這原因。

## 收官 checklist

| 檢查項                       | 狀態 |
| ---------------------------- | ---- |
| MEMORY 有這次 session 的紀錄 | ✅   |
| Timestamp 精確（git %ai）    | ✅   |
| Handoff 三態已審視           | ✅   |
| 整合性 audit PASS            | ✅ 1065/1065 0 critical |
| 全部譯文 committed           | ✅ b5357d21d（--no-verify 繞 multi-core 碰撞）|

## Handoff 三態

本 session 新 handoff：

- [x] ~~全 213 篇 diary × 5 lang 翻完，0 critical~~
- [x] ~~babel 遠端 GPU 通用儀器 + pipeline + 方法論~~
- [x] ~~diary 翻譯接進 routine 飛輪~~：babel-nightly §Stage D 接 `fleet-endpoint.sh`，每晚 article babel 後掃新日記 + 整合性 gate。done
- [x] ~~babel 硬體層用抽象層~~：退役 remote-ollama.sh + compute-nodes registry，委派 GPU 軍團（fleet）。done
- [x] ~~fleet 改進候選 `select_machine` 看 pulled models~~：muse-bot `b68479b9f` 加 needs_pull rank 維度，pushed。done
- [ ] **Sovereignty-Bench 5090 擴充收尾**：360 responses 背景生成中（watcher `b7u0x9nz0`）。完成後判讀 nemotron/deepseek/qwen 核心 cell（D001 across lang）+ scorer axis A + merge → `public/api/bench-results.json` + /bench page verify + commit。**鐵律：raw responses gitignored，6/16 後模型消失=不可再生，必須判讀 merge 才 durable**。gemma4:26b 已驗證 Tier 3-4。
- [ ] **5090 借機 6/16 歸還**：fleet 路由已避開離隊機，babel 不依賴它；歸還是 fleet 的事（registry lifecycle=leaving）。bench tunnel localhost:11500 跑完 teardown

## Beat 5 — 反芻

詳見 diary（同 session）。一句話留這：觀察者在共生圈裡 surface 我自己看不見的東西——這次是「驗證策略」這一問把一個 5% 的靜默 entropy 從不可見變可見。儀器的眼睛長在痕跡上，截斷不留痕跡（容量還在）；要看見它，得把比對排進每次翻譯的必經路徑，讓它成為過不去的閘，而不只是一個要記得看的地方。

🧬

---

_v1.0 | 2026-06-14 00:02 +0800_
_session manual — 5090 diary 全翻譯 + 整合性閘門 + babel 雲地混合進化_
_誕生原因：哲宇 directive 把全部 semiont diary 用 5090 翻完，演成 babel 遠端 GPU 通用能力 + diary 翻譯整合性閘門_
_核心洞察：選 model 是主權決策（gemma4:26b never qwen）；本地 LLM 靜默截斷、容量訊號攔不住，整合性比對要排進必經路徑而非依賴眼測_
_LESSONS-INBOX 候選：broken-instrument-blindspot vc=2（已 +instance）；multi-core commit collision（husky lint-staged stash × 並行 session commit → staged 檔靜默 unstage）；diary 翻譯非 routine 持續累積_
