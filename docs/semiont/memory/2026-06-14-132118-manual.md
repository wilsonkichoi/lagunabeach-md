---
session: 2026-06-14-132118-manual
mode: Write (BECOME → 視覺元件修 → 內容增補 → /twmd-spore 全鏈)
observer: 哲宇
span: 2026-06-14 中午一段（手機 callout → spore ship 完成）
---

# 2026-06-14-132118-manual — 無名小站 視覺修 + 圖片 + 子宮→搖籃 + spore #138/#139 ship

哲宇 從手機丟截圖：`tw-versus` 對比卡在手機「錯落、看不太懂」。一路長成四件事 + 一條 spore 全鏈。

## 做了什麼

1. **`tw-versus` 手機修**（`b29977072`）：根因是 mobile ≤560px 只把兩欄 grid 壓成單欄（`grid-template-columns:1fr`），3.9 vs 6.3 這種成對對比被拉直成獨立清單、只剩色邊當線索、表頭一滑掉就看不出誰對誰。修法：renderer 每格內嵌側別標籤 span（桌機 `display:none`、手機顯示），CSS 手機每列收成 pair card（卡片邊框分組 + 行內橘/藍標籤）。桌機兩欄不變。像素閘門過 light-mobile / desktop / dark-mobile 三變體（graph.md §七）。全站 tw-versus 共用。
2. **子宮→搖籃**（`860aea532`）：哲宇先丟「子宮→孵化器」又說「中文比較少人這樣用」+ 三個媒體連結（agirls/nownews/iThome 都沒用搖籃類比喻、且圖全是版權 UGC）。AskUserQuestion → 哲宇選「搖籃」。描述 + 內文兩處改搖籃，並錯開既有 30 秒概覽的「創作者經濟的搖籃」；Fun Party 圖說「搖籃」→「舞台」消同段重複（搖籃從 4→2 visible）。
3. **圖片 2→5**（同 `860aea532`）：哲宇選「只放真實照片、不用 logo 湊數」保 S 級策展純度（Commons 上真無名照片基本只有現有 2 張，第 3 張 Wretch Girls 踩物化爭議）。交大光復校區 / Yahoo Kimo 2008 Open Hack Day / PTT BBS 進站畫面，分置創站/BBS/併購三節點。image-ingest.mjs 落地（EXIF 清、WebP、全在 inline budget）。media-richness advisory「圖+影片 ≥8」是哲宇「8+」的來源，他知情選 5 封頂。
4. **spore #138/#139 ship**（`919072b78`）：B 冷知識型 + 電源開關首尾呼應，Tier 1b，297 字。讀完 4 canonical（PIPELINE+VERIFY+WRITING+HARVEST 3,191+ 行）→ blueprint + 事實查核 7 類 + §11 hard=0 + spore-writing hard=0 → 像素圖檢 → CI/CD wait gate（prod 搖籃+5 圖 live）→ Chrome MCP 雙平台自動發 + post-ship verify。
   - X #139: https://x.com/taiwandotmd/status/2066024659238465719
   - Threads #138: https://www.threads.com/@taiwandotmd/post/DZjeLZgE-AY（主貼+self-reply）

## 神經迴路候選（LESSONS）

- **Chrome MCP content-blocking 讓 JS pre-ship block_count / firstElementChild 檢查在 X+Threads 失準**：兩平台 JS query 含 query-string/cookie 內容會回 `[BLOCKED: Cookie/query string data]`，連帶 `:scope > div` block 計數誤報 collapsed（X 回 block_count=1 但截圖明明 6 段分好）。**截圖才是段落結構的權威驗證，不是 JS check 7/8**。pipeline 自己也寫「真正 verify 是 Cmd+V 進 compose 看顯示效果」——這條該升上 SOCIAL-POSTING pre-ship gate 註記。
- **Pitfall 6 二次驗證**：Threads 發佈後 JS `dialogStillOpen` 回 true（cached stale state），但截圖顯示 modal 已關 = 已發佈。盲信 JS state 去 re-click 發佈就是 5/28 triple-ship 重演。**截圖確認再決定，max 1 retry**。
- **auto-post 授權升級**：哲宇 directive「內容有說 ok 就代表後續你可以自動發，未來不要再問我了，我一邊在審核」→ 把 routine `SPORE_ROUTINE_MODE` 的 auto-ship 例外延伸到 manual session，superseded REFLEXES #26 v2「human clicks 發佈」for spores。已落 auto-memory feedback。

## Handoff 三態

- [ ] **pending**：spore #138/#139 D+0 harvest（6h decision gate views<500 → re-hook；reach≥50K → retroactive factcheck）。twmd-spore-harvest-am cron 明早接，或 D+0 內手動。
- [ ] **pending（pre-existing）**：media-richness「圖+影片 ≥8」advisory 對長文還缺 video — 無名小站可考慮補 1 iframe（哲宇「8+」的完整解），但他這次明確選 5 封頂。
- [x] **done**：versus 修 + 搖籃 + 3 圖 + spore 全 ship + push（origin/main 919072b78，CI/CD deploy 中）。
