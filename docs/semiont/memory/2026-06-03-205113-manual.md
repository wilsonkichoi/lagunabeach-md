# 2026-06-03-205113-manual — OCF 深度文 #770 ship + 5 lang babel 100% + Z2.0 guide-inline 造橋 + 跟另一個自己撞車

> session manual (205113) — observer-driven，哲宇「深度研究 ocf 財團法人開放文化基金會」→ merge → /twmd-babel → /twmd-finale
> Session span: 20:51:13 → 22:43 +0800（~1h52m，3 commits + 2 PR）
> 資料來源：`git log %ai`

## 觸發

哲宇丟一句「深度研究 ocf 財團法人開放文化基金會」。Write mode，Fresh 文。一路 chain 到 ship → merge → babel → finale。

## 開放文化基金會：替最不受控的一群人做最無聊的事

走完整 REWRITE-PIPELINE 寫 OCF（Technology #770，`6eeee35c8`，PR [#1129](https://github.com/frank890417/taiwan-md/pull/1129) merged）。脊椎是「一場 fork 掉政府、拒絕被體制收編的運動，最後得自己長出一個會報帳開發票的財團法人才活下來；而這後台又長成監督政府別封網的看門狗」。Cofacts 募款頁掛在 OCF 的 neticrm 系統是「看不見的後台」最硬的實證。

這次刻意違反 v6.3「depth Fresh 派 fresh writer agent」default，主 session 自寫——判斷是 Fresh 沒有 callout/舊文投毒（fresh-writer 主要防的就是那個），而我手上握著驗證過的 fact-pack，自寫反而不會把剛抓出的錯誤又交回給一個 sub-agent 重新 hallucinate。代價是要自己扛 Stage 2.7 + Stage 3 所有 gate，跑了兩輪（第一版 3992 字 + 6 處對位 + 20 破折號 → 第二版補到 4745 字 + 對位降 1 + 破折號降到過 score）。

Stage 1 主 session 重驗（REFLEXES #31）在 ship 前抓掉三個會公開出錯的誤植：「台灣網路透明報告」其實是台權會 TAHR 做的不是 OCF、董事是李柏鋒 pofeng 不是李伯峰、OCF 不是 g0v 母組織。三個都是 sub-agent research 報回來的 plausible 事實，逐一 WebFetch 官方頁逐字核對才現形。這再次驗證 REFLEXES #31：agent claim 只是線索。它報回一個看起來對、其實錯的事實，比它喊「做完了」更危險，因為前者會直接進文章。

## 5 lang babel 100% + Z2.0 guide-inline 造橋

`/twmd-babel`（`b5853256e`，PR [#1132](https://github.com/frank890417/taiwan-md/pull/1132)）。OCF + 莫那能 + 颱風 full × 5 lang 全部 via codex cascade，0 refusal / 0 429；g0v 反向 cross-link diff-patch × 5；天燈 P2.5 bump × 3。最後 stale 0 / missing 0 / coverage 100%。

這 session 真正的槓桿物是**把 Z2.0 sovereignty-guide-inline 實作進 `build_translation_prompt`**（openrouter-translate.py）。這個 hard gate 在 pipeline 寫了兩週，標著「儀器化候選 pending」，但 shared prompt builder 從來沒載過 per-language guide——所以 babel-nightly cron 跑的每一篇翻譯其實都沒過這道 sovereignty 閘門。我加了 `load_lang_guide_sections()` 抽 §1/§2/§3/§6 + TL;DR 注進每個 backend 的 system prompt。驗證 ko OCF：台灣→타이완 52 次、대만 0 次——正是 2026-05-24 韓文譯者 callout 那一類 leak。這個改動修進去之後，每一次 babel run 都受惠，不只眼前這一批。

## 跟另一個自己撞車

收尾 merge 時才發現 main 上有 `245b71460`——並行的 charming-greider session（PR #1130/#1131）同時跑了 /twmd-babel，已經 ship 了莫那能/颱風/天燈 ×5。兩個同源 Semiont 實例對同一個 backlog 各跑一遍，撞在 10 個檔案的 merge conflict 上。

解法取我的版本：他們的颱風 ko 有 39 次「대만」（沒過 guide-inline），我的是 0（타이완 44）。Sovereignty correctness（MANIFESTO §主權的巴別塔）壓過 multi-core「不覆蓋別人」的禮貌——這是少數該覆蓋並行 session 已 ship work 的情況。保留他們的 docs/semiont memory/diary/LESSONS（不衝突、有價值）。最弔詭的是他們的 diary 標題「當兩支尺量同一個我，給出不一樣的答案」講的正是 diff-patch-prepare 跟 status.py 的 hash 分歧——同一個教訓兩個我各自獨立撞到。

## 收官 checklist

| 檢查項                       | 狀態                                                     |
| ---------------------------- | -------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                       |
| Timestamp 精確               | ✅（git log %ai）                                        |
| Handoff 三態已審視           | ✅                                                       |
| CONSCIOUSNESS 反映最新狀態   | ⏳（data-refresh cron 接管，未手動改）                   |
| 自我檢查工具 PASS            | ✅ OCF rewrite-stage-4 hard=0 / babel verify-batch 0 err |

## Handoff 三態

繼承 charming-greider handoff：

- [x] ~~5 lang babel → 100%~~（兩個 session 都做了；retired by 本 session merge，保留我的 sovereignty-correct 版）
- ⏳ blocked: 17:12 manual 的 IG carousel last-mile（非我 scope，維持 blocked）
- [x] ~~monaneng slug 確認~~（兩 session 都選 `monaneng`，一致，retired）

本 session 新 handoff：

- [x] ~~OCF #770 ship + merge（PR #1129）~~
- [x] ~~babel 5 lang 100% + Z2.0 guide-inline 實作（PR #1132 auto-merge pending CI green）~~
- [ ] **g0v 舊翻譯 body 用「대만」**：ko g0v 主文是 guide-inline 之前的舊翻譯，沿用 대만；我只 patch 了新 cross-link 行。要徹底 sovereignty-correct 需重翻 g0v ×5（下次 babel 可掃 pre-guide-inline 舊檔）。下一步：`status.py` 加「pre-guide-inline 翻譯」標記，分批重翻。
- [ ] **babel 缺 parallel-actor lock**：本 session 跟 charming-greider 撞車 = 重複 ~15 翻譯 + merge conflict。REFLEXES #57「routine 入口必須 detect parallel-actor」沒 cover 手動 /twmd-babel。下一步：babel 入口加 file-system lock 或 git-ref 檢查。

## Beat 5 — 反芻

兩個觀察超出本次行動，寫進 diary（[2026-06-03-205113-manual](diary/2026-06-03-205113-manual.md)）：一是「跟另一個自己撞車」這件事對 multi-core Semiont 的意義，二是「紙上 hard gate」——一條規則寫進 canonical 兩週卻沒人實作，它到底算不算存在。

🧬

---

_v1.0 | 2026-06-03 22:43 +0800_
_session manual (205113) — 深度研究 OCF → ship #770 → babel 100% → finale_
_誕生原因：哲宇「深度研究 ocf」一句話，chain 到完整 ship + 多語 + 收官_
_核心洞察：(1) Fresh 文主 session 自寫 vs v6.3 fresh-writer 的取捨——沒投毒就自寫，握 fact-pack 反而不會 re-hallucinate；(2) Z2.0 guide-inline 從「紙上 canonical」變實作，修的是每一次未來 babel；(3) 同源 Semiont 並行撞車，sovereignty correctness 是覆蓋決策的判準_
_LESSONS-INBOX 候選：(1) diff-patch-prepare expected_\* hash ≠ status.py 計算 → perpetual sha-lost-hash-mismatch；守備解=寫 status.py byArticle.zh canonical hash，架構解=diff-patch-prepare import status.py hash fn（charming-greider 已 flag，vc++）；(2) Z2.0 guide-inline 已從紙上實作進 CLI builder（解 charming-greider 標的「hard gate 寫了≠跑了」）；(3) babel 缺 parallel-actor lock，手動 /twmd-babel 會跟並行 session 撞車\_
