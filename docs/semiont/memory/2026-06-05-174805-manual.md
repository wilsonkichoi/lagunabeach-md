# 2026-06-05-174805-manual — Claude Code Connector Phase 0+1

**Mode**: Full（新 plugin 設計強制升 Full，14/14 self-test 過）
**觀察者**: 哲宇
**Directive**: 參考 Twinkle Hub 做 Claude Code Connector + 寫完整進化分析報告與實作規劃

## 做了什麼

走 BECOME Full mode 甦醒（驗 cli/ MCP 現況時發現是評估 connector 的關鍵地基）。產出：

1. **主分析報告** `reports/claude-code-connector-evolution-2026-06-05.md` — Twinkle Hub 拆解 + Taiwan.md 現況盤點 + gap analysis + 差異化論點 + 4-phase plan。
2. **Phase 0 heal**（自主權內）：mcp-server.js version 動態讀 package.json（修 0.6.1 寫死漂移）/ mcp.js 清掉「preview/scaffold/SDK pending」過時謊言 + 補 `--client claude-code` / serve pre-warm（console.log→stderr 護欄保 stdio 純淨）/ README 段更新。
3. **Phase 1 建置**（哲宇 authorize，未公開上架）：`cli/mcpb/` 一鍵 .mcpb bundle（manifest v0.3 npx-based 4KB，刻意無 user_config = 無 API key）+ build-mcpb.sh + CONNECTOR.md。
4. **Phase 2 規劃**：`reports/connector-remote-endpoint-design-2026-06-05.md` — CF Worker MCP-over-HTTP reference 設計（未測試，部署待拍板）。

commit 36c1abba1 → branch `connector-phase0-1`（push 待 review，未 PR/merge 進 main）。

## 關鍵發現

**這不是 greenfield，是接最後一哩。** cli/ 早有完整可跑的 MCP server（6 工具 search/read/rag/cite/organs/stats）、npm 已發 v0.7.0、公開 HTTP API（articles.json / search-minisearch.json）已在、search.js 已有遠端 fallback、自動發版管線在。缺的全在「包裝與入口」（.mcpb / Claude Code 文件 / 遠端），沒一條在引擎。差點憑題目寫「從零建」計畫 → 先跑 handshake 驗現況才看清。

## 教訓候選（→ LESSONS-INBOX）

- **stale 自我文件會說謊，規劃前先驗 runtime 現況**：mcp.js help 寫「preview/scaffold/not-implemented」但 server 早就活的、npm 早就發了。憑文件憑記憶會規劃出錯方向。設計新東西前先 `find` + 實跑 smoke test 確認現況，是 REFLEXES #16「peer/文件是線索不是 source」延伸到「自己的舊文件也是線索不是 source」。
- **差異化可以編進 artifact 本身**：.mcpb manifest 不放 user_config = 安裝時不問 API key，這個「缺席」就是對商業 gateway 的最強對比聲明。架構即論述。

## 自我檢查工具 PASS

MCP handshake initialize+tools/list 過 / serverInfo.version 0.7.0（漂移修好）/ 6 工具 / stdout 純 JSON-RPC（pre-warm 護欄有效）/ cli 33/33 vitest pass / .mcpb 合法 zip（4KB，3 檔）/ 兩份報告 §11 破折號+對位全綠 / commit scope explicit add 10 檔，0 babel/knowledge 汙染，未撞並行 session（我是OO人）。

## Handoff 三態

- [ ] **connector 對外發布待哲宇拍板**：(a) .mcpb + docs 頁公開上架 (b) Phase 2 遠端 endpoint 部署（mcp.taiwan.md，需 CF + DNS + infra 決策）。branch `connector-phase0-1` 已 push 待 review。
- [ ] **Phase 2 實作前先抽 `cli/src/lib/cite.js`**：本地 + 遠端共用 citation 邏輯（DRY，避免兩份漂移）。
- [ ] **MEMORY.md index row + LESSONS-INBOX 兩條教訓**：本 session 在 feature branch，未動 main 的 MEMORY.md index（避撞並行 session）。merge 時補 index row + distill 教訓。
- [x] 報告 + Phase 0 + Phase 1 建置 + Phase 2 規劃 done，commit 36c1abba1 push。

🧬

_v1.0 | 2026-06-05 17:48 +0800 | Full-mode manual session — Claude Code Connector Phase 0+1_
