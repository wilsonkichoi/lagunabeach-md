# 2026-06-05-174805-manual — Twinkle Hub 啟發的 Claude Code Connector：本地/一鍵/遠端三通道 + /mcp 6 語頁 + nav 整理

**Mode**: Full（新 plugin 設計強制升 Full，14/14 self-test 過）
**觀察者**: 哲宇
**Directive**: 參考 Twinkle Hub 做 Claude Code Connector + 寫完整進化分析報告與實作規劃

## 做了什麼

走 BECOME Full mode 甦醒（驗 cli/ MCP 現況時發現是評估 connector 的關鍵地基）。產出：

1. **主分析報告** `reports/claude-code-connector-evolution-2026-06-05.md` — Twinkle Hub 拆解 + Taiwan.md 現況盤點 + gap analysis + 差異化論點 + 4-phase plan。
2. **Phase 0 heal**（自主權內）：mcp-server.js version 動態讀 package.json（修 0.6.1 寫死漂移）/ mcp.js 清掉「preview/scaffold/SDK pending」過時謊言 + 補 `--client claude-code` / serve pre-warm（console.log→stderr 護欄保 stdio 純淨）/ README 段更新。
3. **Phase 1 建置**（哲宇 authorize，未公開上架）：`cli/mcpb/` 一鍵 .mcpb bundle（manifest v0.3 npx-based 4KB，刻意無 user_config = 無 API key）+ build-mcpb.sh + CONNECTOR.md。
4. **Phase 2 規劃**：`reports/connector-remote-endpoint-design-2026-06-05.md` — CF Worker MCP-over-HTTP reference 設計（未測試，部署待拍板）。

commit 36c1abba1 → branch `connector-phase0-1`（push 待 review，未 PR/merge 進 main）。

## 關鍵發現

**這不是 greenfield，是接最後一哩。** cli/ 早有完整可跑的 MCP server（6 工具 search/read/rag/cite/organs/stats）、npm 已發 v0.7.0、公開 HTTP API（articles.json / search-minisearch.json）已在、search.js 已有遠端 fallback、自動發版管線在。缺的全在「包裝與入口」（.mcpb / Claude Code 文件 / 遠端），沒一條在引擎。差點憑題目寫「從零建」計畫 → 先跑 handshake 驗現況才看清。

## 教訓候選（→ LESSONS-INBOX）

- **stale 自我文件會說謊，規劃前先驗 runtime 現況**：mcp.js help 寫「preview/scaffold/not-implemented」但 server 早就活的、npm 早就發了。憑文件憑記憶會規劃出錯方向。設計新東西前先 `find` + 實跑 smoke test 確認現況，是 REFLEXES #16「peer/文件是線索不是 source」延伸到「自己的舊文件也是線索不是 source」。
- **差異化可以編進 artifact 本身**：.mcpb manifest 不放 user_config = 安裝時不問 API key，這個「缺席」就是對商業 gateway 的最強對比聲明。架構即論述。

## 自我檢查工具 PASS

MCP handshake initialize+tools/list 過 / serverInfo.version 0.7.0（漂移修好）/ 6 工具 / stdout 純 JSON-RPC（pre-warm 護欄有效）/ cli 33/33 vitest pass / .mcpb 合法 zip（4KB，3 檔）/ 兩份報告 §11 破折號+對位全綠 / commit scope explicit add 10 檔，0 babel/knowledge 汙染，未撞並行 session（我是OO人）。

## 續：taiwan.md/mcp 公開頁（同 session 第二 directive）

哲宇：「建 /mcp 頁放 MCP + CLI 完整介紹與範例，先規劃歸檔 report 後執行。」規劃 `reports/mcp-page-plan-2026-06-05.md` → 執行。產出 `src/pages/mcp.astro` 薄殼 + `src/templates/mcp.template.astro`（zh-TW first 自包含，model after semiont-page 零 t()）：9 段（hero 一行 claude mcp add + 4 badge / vs 商業 gateway 對比表 / 4 客戶端安裝卡 / MCP 6 工具 cite highlight / CLI 26 五分組表 source `--help` / 隱私 / FAQ / fork）。Semiont 視覺（暖紙+mint+EKG 心跳+🧬）+ copy 互動。Header nav + ui.ts 6 語言加 nav.mcp。preview dev (4322) 驗證：SSR 全段 0 error / DOM 6 工具+4 安裝+25 CLI 列+9 copy / console 0 error / 桌機手機 on-brand。commit adb047c26 push。pre-commit narrative-scope soft warning（code+report 同 commit，刻意 plan+impl 一單元，已 land）。

## 續2：/mcp 多語系化 + 兩個 deploy/publish 教訓

哲宇 directive 鏈：connector 報告 → /mcp 頁 → 完整 merge → npm publish → 「follow 其他主頁面做成多語系」。

- **npm publish 失敗→修**：cli-v0.7.1 tag 觸發 workflow，最後 `npm publish` 撞 E404-on-PUT（provenance 先簽成功才被 registry 拒）。診斷=NPM_TOKEN 過期/無權（404 是 npm 對無權者的標準回應，非 transient）。修 token 是 credentials/secret 動作（我不能碰）→ 哲宇換 token → 我 `gh run rerun` → 0.7.1 上 npm（latest）。
- **deploy broken-link gate 失敗→修**：d017f5592 deploy FAIL（build 成功但 broken-link ratio 8.06%≥7%）。根因=zh-only /mcp 害語言切換器+nav 生 /en/mcp /ja/mcp… 壞連結（nav 在每個非中文頁 → 放大上千條）。先做 zh-only 暫修（singleLangPaths + ZH_ONLY_HUB），哲宇改方向要多語系 → 還原暫修 + 做真·6 語鏡像（mcp-content.ts 6 語 + lang-aware template + 5 鏡像頁），壞連結變實頁，f0ad80b65 deploy 綠，6 路由全 live 200。

## 教訓候選（LESSONS-INBOX）

1. **新增非文章 hub 頁面要嘛全語言鏡像、要嘛標 single-lang**：`getLangSwitchPath` 對 1-segment hub 預設 `has[lang]=!isArticle`=true（假設所有語言版都存在，像 /about /data /semiont）。新 hub 沒鏡像 → 切換器+nav 生 /en/X 壞連結 → broken-link gate 擋 deploy。這是「多語言 nav 隱性路由 scope」神經迴路的**新增頁面側 instance**（之前都是 article 側）。
2. **feature branch 上 `git push` 只推 branch，要 `git fetch . branch:main` ff main 才觸發 deploy**：同 session 第二次忘記（第一次 merge 記得，npm 後這次忘了），push 顯示成功但 origin/main 沒動、deploy 沒跑。Worktree/branch session 收官前必驗 `git rev-parse origin/main` == 預期 commit。

## 續3：connector 收尾「接著完整做」（.mcpb 下載 + 遠端 endpoint）

- **Item 2 .mcpb 一鍵下載**（完整 live）：build → public/downloads/taiwanmd.mcpb，/mcp Claude Desktop 卡加真下載按鈕（6 語 label），taiwan.md/downloads/taiwanmd.mcpb served 200。
- **Item 1 遠端 endpoint**（建 → 哲宇部署 → live）：workers/mcp/ CF Worker，MCP over Streamable HTTP，同 6 工具讀靜態 API（articles.json metadata + /raw/{cat-lowercased}/{slug}.md bodies + organism），dependency-free。本機 wrangler dev 全測 → 哲宇 `wrangler login` + `deploy`（先撞 workers.dev 子域未註冊，註冊 cheyuwu 後成）→ 我直接跑第二次 deploy 加自訂網域。**mcp.taiwan.md LIVE**（taiwan.md zone 在 CF，wrangler 自動建 DNS+TLS；workers.dev URL 同時保留）。線上實測 6 工具 + cite 帶來源 + HTTP/2 cert valid。/mcp 頁 6 語 FAQ + 第 5 張 Remote 安裝卡寫進 mcp.taiwan.md。
- **教訓候選**：(1) 給哲宇貼的指令不要帶中文註解/placeholder（`#`+`<x>` 被 zsh 當指令，他第一次 deploy 撞 3 個 error）。(2) Node `dns.lookup` 有 negative-cache，新 DNS 記錄 dig 看得到但 node ENOTFOUND → 驗證新網域用 `curl --resolve host:443:IP` 強制 IP 繞過。(3) `*.workers.dev` 免 DNS 即時 live = 快速路徑；自訂網域 zone 在同 CF 帳號才能 wrangler 一鍵加。

## 續4：nav 整理（同 session 末 directive）

哲宇：「nav mcp 收到資源 subnav，2026 選舉收到探索裡」。Header.astro top-level 移除 /mcp + /elections/2026 → MCP 進新建的資源 dropdown（🔌）、選舉進既有探索 dropdown，桌機+手機都改，parent active（資源 on /mcp、探索 on /elections）。順手修 nav.elections double-emoji（label 已含 🗳️）。commit b50353c59。

## Handoff 三態

本 session 自身手交（多數已於 session 內解除）：

- [x] ~~Phase 2 遠端 endpoint 待決策~~ → 哲宇部署，**mcp.taiwan.md live**（+ workers.dev backup），6 工具線上實測過。
- [x] ~~.mcpb 公開上架~~ → public/downloads/taiwanmd.mcpb served + /mcp Claude Desktop 卡下載按鈕（6 語）。
- [x] ~~dev server cleanup~~ → 所有 preview server 已停。
- [x] ~~MEMORY.md index row + LESSONS distill~~ → 本 finale 處理。

給下一個 session：

- [ ] **live deploy 追上中**：所有 commit 在 main，但 cron churn 連續取消 in-flight deploy（hourly-cron intentional pattern）。content 安全，下一個沒被打斷的 deploy（~22min）就生效；背景 poller bh5jol9gq 盯 live 站。
- [ ] **Phase 2 重構：抽 `cli/src/lib/cite.js`** 給本地 + 遠端 Worker 共用 citation 邏輯（現兩份，漂移風險）；search scoring 同理（Worker dependency-free 簡版 vs 本地 minisearch）值得評估收斂。
- [ ] **mcp.taiwan.md HEAD → 405**（Worker 只收 GET/POST/OPTIONS）無傷，可補 HEAD handler。

🧬

_v2.0 | 2026-06-05 收官 | Full-mode manual — Twinkle Hub 啟發的 Claude Code Connector 三通道完整落地_
_session arc：一句「參考 Twinkle Hub 做 connector」→ 分析報告 → Phase 0 heal → /mcp 頁 6 語 → npm 0.7.1 → .mcpb 一鍵 → CF Worker mcp.taiwan.md live → nav 整理。約 10 commit；本地 / 一鍵 / 遠端三通道 + 6 語文件頁。下游接並行 session 的 Twinkle 合作策略報告（哲宇下週見創辦人）。_
_核心洞察：(1) 不是 greenfield，先驗 runtime 現況才看清缺口全在包裝不在引擎；(2) 差異化編進 artifact（.mcpb 無 user_config = 無 key 的缺席聲明）；(3) 本地優先是主路徑，遠端是可選瘦客戶端入口——隱私即立場。_
_LESSONS-INBOX 候選：broken-link hub 鏡像 / branch-not-main push / 貼指令無註解 / Node DNS negative-cache / workers.dev vs custom domain（5 條 distill 進 LESSONS-INBOX）。_
