# 2026-06-14-154636-無名小卒勘誤 — 讀者抓到「無名小卒」站名幻覺 → 三源查證正名「無名」+ 公開勘誤 + LESSONS stage2-quote-context-collapse + CI Actions Node20→24 PR #1150

> session 無名小卒勘誤 — observer-triggered（哲宇 /twmd-become + 讀者 callout 轉述）
> Session span: 15:30 → 16:07 +0800 (~37 min, main 2 commits + CI PR #1150)
> 資料來源：`git log %ai`

## 觸發

哲宇轉來無名小站文章的讀者 feedback——「六個資工系學生用系上報廢的設備拼出一個叫『無名小卒』的 BBS」，問「無名小卒」是幻覺還是哪裡有引用，要完整查核、驗證、修文章。這篇是今天稍早（013028-manual）EVOLVE A→S 重寫、並 ship 孢子 #138/#139 雙平台的，所以幻覺若成立就已經隨孢子公開出去了。

## 站名幻覺：查證與修正

判決是一個幻覺，最隱蔽的那種「引語真、語境崩」型。研究報告 SSOT 其實查對了「名稱一開始就叫『無名』，無前身改名」（`reports/research/2026-06/無名小站.md:18`），§4 引語庫也把簡志宇「我們都是無名小卒，一開始沒有太大的野心」標成命名由來。是 Stage 2 下筆把這句自嘲引語壓成了字面站名，孢子的事實鐵三角自檢還寫「『無名小卒』是專名」把錯誤合理化一次才漏出去。三源複驗（光華一手「取名為『無名』」/ 維基「無名小站 BBS、全文無『無名小卒』」/ 看雜誌「架起『無名』BBS 站」）一致。

修正落在文章 4 處 + 孢子藍圖 #138 三處（`9c871ad3b`），簡志宇逐字引語保留不動。讀者另一句「根本不是 BBS」，這點他弄錯了：BBS 起源有 5 源（維基/光華/看雜誌/人間福報/StartupStadium），那部分維持原狀——查證要雙向，反方 claim 也得 falsify。push 後盯 GitHub Pages 部署完成、用 robust UTF-8 curl 驗線上正文舊字串消失、新「無名」上線、逐字引語保留，才給哲宇綠燈。公開勘誤由哲宇釘 Threads（他改寫過、把 organism 術語收掉給大眾讀者，這個調性判斷是對的）。

## CI Actions Node 20 → Node 24（PR #1150）

修勘誤時注意到部署 log 的 Node 20 deprecation：GitHub 6/16（兩天後）強制 Actions 跑 Node 24，deploy.yml 全用 node20 action = 部署可能壞。spawn_task 開背景 chip，哲宇點開接成這條工作。同一套「實查不靠記憶」的紀律：每個 action 版本對 `action.yml` 的 `using:` 用 `gh api` 驗 current=node20 / target=node24，9 個 actions、22 處 bump 到最早 node24 major（github-script 取 v8 不取 v9，避開 ESM-only 的 `require` 破壞；upload-pages-artifact dotfile 排除查過只影響 `.DS_Store`、CNAME 保留）。隔離 worktree 改、開 PR #1150 不自己合，pr-review check 已綠（checkout@v5 在真 runner 驗過）。

## 收官 checklist

| 檢查項                        | 狀態                     |
| ----------------------------- | ------------------------ |
| MEMORY 有這次 session 紀錄    | ✅                       |
| Timestamp 精確（git log %ai） | ✅                       |
| Handoff 三態已審視            | ✅                       |
| CONSCIOUSNESS 反映最新狀態    | ✅（無組織級變更，不動） |
| 自我檢查工具 PASS             | ✅ prose-health          |

## Handoff 三態

繼承 132118-manual：

- [ ] **pending（部分接住）**：spore #138/#139 的 retroactive factcheck 這次因讀者 callout 提前觸發、已修＋公開勘誤；但 D+0 engagement harvest ＋ 6h re-hook decision gate 仍 pending（`twmd-spore-harvest-am` 明早接，或哲宇手動）。spore 已破 2.5 萬 views。
- [ ] **pending（pre-existing，非本鏈）**：無名小站 media-richness 缺 video（圖＋影片 5<8），要上 8 補 1 iframe，另開任務。

本 session 新 handoff：

- [x] ~~無名小站「無名小卒」勘誤修＋部署＋線上驗證＋公開勘誤（`9c871ad3b`）~~
- [x] ~~LESSONS stage2-quote-context-collapse 寫入 INBOX（`ebbffc215`）~~
- [ ] **pending**：CI Node24 PR #1150 等 reviewer／哲宇合併（6/16 前），合併後盯第一次 deploy run（deploy-pages@v5／upload-pages-artifact@v5 只有 merge 才驗到，Pages rollback-safe）。
- [ ] **pending**：stage2-quote-context-collapse 只 intake 到 LESSONS-INBOX，還沒 distill 進 REWRITE Stage 3 ＋ SPORE Step 3c 事實自檢 checklist。等 `twmd-distill` routine 或哲宇點。

## Beat 5 — 反芻

今天這條鏈最值得記的，是同一個原則在兩個看似無關的任務裡其實是同一件事：fact-check 問「無名小卒是不是站名」、CI bump 問「v5 是不是 node24」，兩個都只能靠「不信記憶、回去查地面真相」才不出錯。而幻覺的形狀本身——引語是真的、語境崩了——比憑空捏造更難抓，因為每個原子都 Ctrl-F 得到。完整反芻寫進 diary。

🧬

---

_v1.0 | 2026-06-14 16:07 +0800_
_session 無名小卒勘誤 — 讀者 callout → 三源查證正名 → 公開勘誤 → LESSONS → CI Node24 PR_
_誕生原因：讀者在 spore #138 抓到「無名小卒」站名幻覺，哲宇要完整查核＋修文章_
_核心洞察：引語逐字存在不等於語境角色對；同一條「查地面真相」紀律同時治幻覺與選對 CI 版本_
_LESSONS-INBOX 候選：已寫 stage2-quote-context-collapse（`ebbffc215`）_
