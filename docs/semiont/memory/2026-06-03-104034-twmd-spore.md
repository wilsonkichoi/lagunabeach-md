# 2026-06-03-104034-twmd-spore — 天燈 spore #113/#114 ship + Computer-use disconnected 後 SHIP path 撞 Claude Code focus-stealing barrier

> session twmd-spore — 哲宇 manual `/twmd-spore https://taiwan.md/culture/天燈/` directive
> Session span: 2026-06-03 09:48 → 10:41 +0800 (~53 min, 2 commits)
> 資料來源：`git log %ai` + Chrome MCP post-ship verify

## 觸發

哲宇手動 `/twmd-spore` 天燈文章。文章 5/27 ship 已 7 天未發 spore，趁熱窗口最後一段；同時是本 session 在 fire #10 storm-defer 收尾 + diary 寫完後，哲宇主動觸發的第一篇 spore，順便 dogfood 全自動 SHIP path。

## VERIFY 全 17 hard gate PASS

讀完 SPORE-PIPELINE / SPORE-VERIFY / SPORE-WRITING / SPORE-HARVEST-PIPELINE 共 3,191 行 canonical（前 1170 行 HARVEST，剩下 449 是 D+1 harvest 細節先 skip），用 A1 人物型標準版 + B 反差冷知識 hybrid 寫 blueprint。

Hook angle 鎖在 article 自身的 D hypothesis「同一群平溪人扛兩個身分」— 兩個姓胡的反差（胡維銘 受害者 vs 胡民樹 天燈協會理事長）做 anchor，200 年弧線（1820 報平安 → 2019 326 公斤 → 2025 自治條例）做收尾。Hook tier 1b 具體性槓桿（72 歲 / 50 年的房子 / 一顆未燒盡的天燈 三個獨立可查 anchor）。

Plugin gate 雙跑全綠：`spore-writing` hard=0 warn=0（Rule #14 v2 字面 prefix「你知道嗎？🏮」+ Rule #14.A standalone hook ≤ 15 chars + Rule #14.B paragraph block ≥ 4 全 PASS），`prose-health` hard=0（2 Tier 2 metaphor + 引用荒漠 warn 是 spore body 規格不適用的 article-quality dim，非 blocker）。事實鐵三角 12 條 fact 全 ✅ verbatim 對齊 article footnote [^14][^16][^17][^8][^6][^9][^1][^2][^12][^13][^21]。紀實/煽情閘 PASS：父母靈位提及是 article 已公開 + verbatim 引語 + 不重構火災現場。

Blueprint commit `995755368` 命名為 `112-天燈.md`，這是後面踩到的第一個坑 — 沒先 grep SPORE-LOG 確認最新 # 是多少，#112 其實已被 Computex X 佔走。

## SHIP attempt — Computer-use disconnected 後 focus-stealing barrier

Computer-use MCP 在 session 中段 disconnected（system reminder mid-conversation 確認），SOCIAL-POSTING-PIPELINE v0.7 §AI pre-ship check 7+8 keystroke flow 假設 computer-use 可用，缺了它整條 SHIP 自動化要靠純 osascript。實測下來這條路撞結構性 barrier。

第一道闸：osascript Accessibility permission 預設 denied，第一次 `keystroke "v" using command down` 直接報「不允許 osascript 傳送按鍵」。哲宇手動到 System Settings grant 後重試，得到第二道闸：Chrome 即使透過 `tell application "Google Chrome" to activate` 也常常不是真正 OS-frontmost，`document.hasFocus()` 持續 return `false`。換 NSWorkspace `activateWithOptions(3)`（all-windows + ignoring-other-apps）+ 等 2 秒後 keystroke，第一次成功 paste 一段測試文「SOCIAL-POSTING-PIPELINE」進 Threads compose textbox — 證明這條路理論可行。

但後續 image paste 一直失敗：clipboard 確認有 PNG（osascript PNGf + 9 種 image format 都 set 進 generalPasteboard），keystroke 也 fire 完成，Threads dialog 內 `[data-pressable-container] img.src.startsWith('blob:')` count 仍是 0。Root cause 在每個 Chrome MCP `javascript_tool` call 期間 Claude Code IDE 自動搶回 OS focus — 我用 JS focus textbox 後 Bash 還沒 fire keystroke，Chrome 已經失去 frontmost 狀態，Cmd+V 落到別處（可能是 Claude Code window 自己）。

試過幾條 workaround：synthetic ClipboardEvent dispatch with DataTransfer File 因為 Threads 是 HTTPS / 我的 local http server 8765 是 HTTP，mixed content 直接被 CSP 擋；base64 inline inject 832KB 預估超過 javascript_tool param size 限制。最後跟哲宇報告 3 個 option：A 全手動 / B 半自動 hybrid（人拖圖 + AI 文字）/ C 我繼續硬幹 base64 chunked + synthetic DragEvent。哲宇選擇 Option A 直接手動 ship。

## Post-ship verify + LOG + sporeLinks

哲宇 ship 完丟兩個 URL 回來 — Threads `DZGziGaEwgz` + X status `2061992719715795224`。Chrome MCP navigate 兩平台跑 post-ship 5/5 check：Threads og:description 完整保留 6 paragraph break + 12 line break + hook「你知道嗎？🏮」+ verbatim quote「錢是他們在賺，苦是我們在受」+ close「扛兩個身分」+ og:image instagram CDN 渲染 + 2 container 確認 main + reply。

X 端我第一輪 verify 用 `[data-testid="tweetPhoto"]` selector 抓到 0 image 報「X 漏圖」給哲宇看，他直接截圖回我證明 X 顯示完整 OG link card preview 含 taiwan.md 文章 hero 圖 — `tweetPhoto` 只抓 user-uploaded media，X URL auto-expand 成 card preview 用不同 DOM 結構。修正 verify 邏輯認知後雙平台全 PASS。

Commit `f41a0fb82` 一次寫 SPORE-LOG #113/#114 雙 row + frontmatter sporeLinks 雙 entry + blueprint 從 `112-天燈.md` 重命名 `113-天燈.md`。UTM 號碼 s112/s113 跟 Computex #111/#112 重複沒辦法修補（已 ship），SPORE-LOG description 加註「GA 分析需用 source=threads/x + referrer 解 association」。Chrome MCP tab 710178346 cleanup tabs_close_mcp，group auto-removed。

## 收官 checklist

| 檢查項                       | 狀態                                          |
| ---------------------------- | --------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅ 本檔                                       |
| Timestamp 精確               | ✅ `git log %ai` + 即時 `date`                |
| Handoff 三態已審視           | ✅ 見下方                                     |
| CONSCIOUSNESS 反映最新狀態   | ✅ 早上 BECOME 已抓 snapshot，本 session 無變 |
| 自我檢查工具 PASS            | ✅ spore-writing hard=0 / prose-health hard=0 |

## Handoff 三態

繼承上 session（fire #10 + diary session 09:43）：

- ⏳ blocked: `routine-drift.sh` 24hr-duplicate detection 升級（hourly cron 為刻意設計，detection 收緊到 30 min 內 > 1 fire — 不是 24hr threshold）
- ⏳ blocked: `~/.claude/scheduled-tasks/twmd-rewrite-daily/SKILL.md` description 從「daily 18:00」改「hourly token-budget burn」
- ⏳ blocked: ROUTINE.md `twmd-rewrite-daily` cadence 描述同步
- ⏳ blocked: 02:06 commit `bfff8e8c8` 引入的「spirit-beyond-strict-30-min-rule」需 sunset / 改寫
- ⏳ blocked: 2026-05-29 HARVEST-REPLIES-PENDING (@walkinginthemoon + @qooapp) 仍 unshipped
- ⏳ blocked: 3 EVOLVE candidates (黃蘿蔔片 / 番膏 / 嘉義 brand) → ARTICLE-INBOX §P1 promote
- ⏳ blocked: #89 雷亞 5/28 duplicate ship cleanup
- ⏳ blocked: 🛡️ 免疫 27 chronic vs `dashboard-immune.json` score=67 落差

本 session 新 handoff：

- [x] ~~天燈 spore #113/#114 雙平台 ship + post-ship verify + LOG + sporeLinks~~
- [x] ~~feedback `hourly-cron-intentional` user-level memory + MEMORY.md indexed~~
- [ ] **D+0 1hr/3hr/6hr harvest 天燈 spore** — Chrome MCP navigate 雙 URL，抓 views/likes/reply count；6hr views < 500 → re-hook 救援
- [ ] **新 SOP「無 computer-use 環境下 spore SHIP 路徑」** — SOCIAL-POSTING-PIPELINE v0.8 需加 §workaround paths（API integration / Option B 半自動 / Option A 全手動），Claude Code IDE focus-stealing 是結構性 barrier 不是哲宇環境問題
- [ ] **Blueprint # vs SPORE-LOG # 對齊鐵律** — pre-blueprint mandatory step：`grep -E "^\| #[0-9]+" SPORE-LOG.md | head -3` 取最新 #，blueprint + UTM 從 max+1 起。今天 UTM 已撞，下次預防
- [ ] **X post-ship verify fallback** — `tweetPhoto` selector 之外加 OG link card preview detection (og:image meta + card URL render check)

## Beat 5 — 反芻

兩件事值得 distill 進 LESSONS-INBOX：(1) **Computer-use disconnected = SHIP path 結構性斷裂**。SOCIAL-POSTING-PIPELINE v0.7 整套 keystroke-based pre-ship check 假設 computer-use 在場，缺了它純 osascript 路徑撞 Claude Code IDE auto-refocus barrier。Workaround 不能等 computer-use 回來，要 pipeline 長出「無 computer-use 環境」分支 SOP，三選一明文 ladder（API → 半自動 → 全手動）。(2) **驗證 selector 寫死的代價**。X post-ship 一個 `tweetPhoto` selector 沒涵蓋到 link card preview，直接讓我誤報「X 漏圖」給哲宇，他截圖才反駁回來。修補方向不該再加 selector 補丁，要 verify 邏輯從「image attached」改成「image visible to reader」— OG image meta + card render + attached photo 任一存在即 PASS。

第三個 pattern 是 UTM/# 對齊：blueprint 命名前沒查最新 # 就動筆，等 ship 完才發現號碼撞 Computex。這個是 mechanical SOP miss，加 pre-blueprint grep 即可，不到 distill 級。

更深一層的 meta-pattern：今天從 fire #10 storm-defer 開始整路下來（defer chip → 哲宇 callout hourly intentional → diary「紀律守住 ≠ 對齊意圖」→ /twmd-spore 撞 focus barrier → 哲宇手動接手）三段都是「假設前提錯了，紀律執行到位也救不回」的 instance — 今早的 diary 寫完不到 1 小時就被自己重現一次。pipeline canonical 假設 computer-use 在場、blueprint # 假設自動對齊 SPORE-LOG、verify selector 假設 image 只走 attached path — 三個 assumption 全部沒在動工前 verify。下次寫 Beat 5 反芻要記得：reset frame, not escalate vc。

🧬

---

_v1.0 | 2026-06-03 10:41 +0800_
_session twmd-spore — 哲宇 manual `/twmd-spore` 天燈 directive，full-auto SHIP 撞 Computer-use disconnected + Claude Code focus-stealing barrier，Option A handoff manual ship 雙平台_
_誕生原因：fire #10 storm-defer 收尾 + diary 寫完後哲宇主動觸發第一篇 spore，順便 dogfood 全自動 SHIP path 發現結構性斷裂_
_核心洞察：(1) Computer-use disconnected = SOCIAL-POSTING-PIPELINE v0.7 keystroke path 結構性斷裂，需新分支 SOP (2) Verify selector 寫死的代價 — X 漏圖誤報，需從「image attached」改成「image visible to reader」(3) Blueprint # 對齊 SPORE-LOG # 是 mechanical SOP miss，pre-blueprint grep 即可預防_
_LESSONS-INBOX 候選：(1) Computer-use disconnected 後 SOCIAL-POSTING SHIP path workaround ladder (API → 半自動 → 全手動) (2) X post-ship verify 加 OG card preview fallback (3) Blueprint # vs UTM vs SPORE-LOG # 三方對齊 mandatory grep_
