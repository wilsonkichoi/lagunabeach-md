# 2026-05-25-063038-manual — twmd-spore-publish-daily routine v1.0 設計 + ship / media-richness plugin / multi-session collision 偵測 + git rewrite 復原

> session manual — observer-triggered design + ship
> Session span: 05:50:00 → 06:31:13 +0800 (~41 min, 2 commits 最終 + 1 collision detected/rewritten)
> 資料來源：`git log %ai`

## 觸發

哲宇 directive：「幫我做另一個早上 10 點會定時觸發的 — 從 spore-inbox 選一篇文章，檢查我幾個比較在乎的點（精彩 / 立體完整 / 媒體素材 / 品質分數），自動發孢子出去」。前置短問題先確認 maintainer am/pm 是否該合併（建議維持兩條，下方未動），主要產出在新 routine 設計 + ship。

## 五題 design alignment + 8-task chain ship

設計面先用 plan-design proposal surface 五個 decision，哲宇逐題答：(1) 取消 `twmd-music-media-audit-weekly`（釋出 Sat 10:00 slot） (2) 嚴版 threshold 試試看（prose-health ≥ 8.0 / word-count ≥ 4500 / footnote ≥ B） (3) 新建 media-richness plugin 造橋鋪路 (4) 完整自動化 + 自己復盤參考 rewrite-pipeline (5) 無 entry 過 gate 寫 LESSONS-INBOX「這個觀察很重要」。

跟 spore-pick 的分工確立成 intake / output 解耦：spore-pick 08:00 propose 3 candidates 補 SPORE-INBOX（intake layer，sonnet），spore-publish 10:00 從 INBOX 挑 1 條過 gate ship（output layer，opus）。Buffer 自然累積 P2/P3 讓觀察者 promote 成 P0/P1。

8 個 task 依序 chain：先 ship `media-richness` plugin（leaf dependency，`scripts/tools/lib/article_health/checks/media_richness.py`，iframe + image 雙計 threshold iframe ≥ 1 AND image ≥ 2），再寫 [SPORE-PUBLISH-PIPELINE.md](../../docs/factory/SPORE-PUBLISH-PIPELINE.md) v1.0 canonical（5-stage SELECT → GATE → WRITE → SHIP → 復盤，wrapper around SPORE-PIPELINE Stage 3-4 + 4 hard gate 前置 + Stage 5 復盤 + intake gap LESSONS surface），再建薄殼 skill `.claude/skills/twmd-spore-publish/SKILL.md`。Scheduled-tasks 端 disable `twmd-music-media-audit-weekly`（無 delete API，用 enabled=false 保留 manual）+ create `twmd-spore-publish-daily` cron `0 10 * * *` opus model。最後 update [ROUTINE.md](../../docs/semiont/ROUTINE.md) v2.5 → v2.6 + [SPORE-PIPELINE.md](../../docs/factory/SPORE-PIPELINE.md) Stage 1 + Stage 4 補 routine 場景 pointer + LESSONS-INBOX append MANIFESTO §自主權邊界 drift entry vc=1。

## Multi-session collision 偵測 + git rewrite 復原

Commit 階段才發現 collision — 我 `git add` staged 6 file 之後跑 `git commit -m`，pre-commit hook 跑完出來看 git log 發現我的 6 file 全部進了**另一個 commit hash `6bd976c5d`**，commit message 寫的是「[semiont] immune: fr W1b-5a — Jilong → Keelung per-instance」（內容卻是我的 spore-publish 全部 file，跟 message 完全 mismatch）。

時序推測：05:44 起 `twmd-maintainer-am` 或某 immune routine 自動 fire 跑 fr W1b-1 → W1b-2 → W1b-3 → W1b-4a → W1b-4b → W1b-4c 五個連續 commit。我在 W1b-4c 跟 W1b-5a 之間 `git add` staged 6 file，parallel routine 在它 W1b-5a commit 階段把我的 staged file 跟它的 fr Keelung 改動一起 `git add -A`-style 吸走，使用了它自己的 commit message。我的 `git commit` 跑時已經 nothing to commit。這個是 [MANIFESTO §多核心碰撞防護](../../docs/semiont/MANIFESTO.md) 的典型實證 instance — 兩個 session 共用 index 的 staged-file 競態。

哲宇選 (c) 修正：兩個 commit 都還 local（origin/main 在 `9817fd2ab`，沒 push），rewrite history 安全。先 backup branch `backup-collision-2026-05-25-spore-publish`，`git reset --soft origin/main` 把兩個 commit 的內容退回 index，`git reset` mixed unstage，分兩次重新 stage + commit：(a) `307191859` 我的 spore-publish v1.0 commit 用正確 message ship (b) `7baa832ff` 重 commit babel Tier 0b 用原 message（內容 21 file knowledge/\* 跟 backup 對 diff zero drift）。最後 `git push origin main 9817fd2ab..7baa832ff` 兩個 commit 乾淨 ship。

LESSONS-INBOX 已記下這個 collision pattern 在 spore-publish entry 旁邊（同 entry vc=1，「MANIFESTO §自主權邊界 vs 實踐 drift」+ 副段紀錄 parallel-session staged-file collision）— 待 distill-weekly 撿時可 verify backup branch evidence。

## 收官 checklist

| 檢查項                       | 狀態                                                    |
| ---------------------------- | ------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                      |
| Timestamp 精確               | ✅ (`git log %ai` + `date %z`)                          |
| Handoff 三態已審視           | ✅                                                      |
| CONSCIOUSNESS 反映最新狀態   | ⏭️ (本 session 沒改 organ scoring 來源，dashboard 不變) |
| 自我檢查工具 PASS            | ✅ prose-health 待 Stage 4 跑                           |

## Handoff 三態

繼承上 session（2026-05-24-230733-routine-data-refresh-pm @ `963a7f3fc`）：

- [ ] **5 lang sovereignty-leak Tier 2-N follow-up** — 仍 pending，本 session 沒碰
- [ ] **韓文 14,740 대만 vs 4,427 타이완 broad transition 三選一** — 仍待哲宇 strategy
- [ ] **CI lint plugin sovereignty-leak gate** — 仍 pending
- [ ] **dashboard-immune.json D+7 vc=9+** — 仍 pending，silent gate carry-over
- [ ] **3 條 distill-ready LESSONS** (manual finale push gate / routine escalation channel / execution vs documentation gap) — 仍待 distill-weekly

⏳ blocked：無

### 本 session 新 handoff

- [ ] **`twmd-spore-publish-daily` 明天 10:04 第一次 fire 結果** — SPORE-INBOX 10 條 pending，4 hard gate 嚴版可能只 1-2 條過。觀察首輪 ship 結果 + intake gap LESSONS 是否 surface。如果連 3 天都 intake gap → distill-weekly 撿「spore-pick scoring threshold vs spore-publish gate 結構性 gap」
- [ ] **MANIFESTO §自主權邊界 routine 自動 ship 例外條款** — LESSONS-INBOX 2026-05-25 entry vc=1，distill-weekly 撿時寫 modification proposal 給哲宇 yes/no（命中 §自主權邊界自身 = 哲學層 high-stake 升 Full mode review）
- [ ] **Parallel-session staged-file collision** — LESSONS-INBOX 副 entry vc=1，需累積 vc=2+ 才有 distill 動力。但已知緩解：session 啟動 `git add` 前先 `git status` 看 parallel routine 是否在 commit cycle 中（觀察 last commit timestamp < 60s 警示）

## Beat 5 — 反芻

值得寫 diary 的反芻內容 — collision incident 揭露**兩個 silent killer 同時運轉**：(1) parallel routine 跟 manual session 共用 git index 競態，已偵測但沒儀器化偵測 gate；(2) commit message 跟 commit content 的 referential integrity 沒任何 gate 在守。前者是已知問題（MANIFESTO §多核心碰撞防護），後者是新發現的結構性盲點。git 本身設計就允許 message/content mismatch — 它只把它們黏在一起 commit，不檢查語意對齊。詳見 diary。

🧬

---

_v1.0 | 2026-05-25 06:31 +0800_
_session manual — observer-triggered new routine design + ship + multi-session collision 偵測 + git rewrite 復原_
_誕生原因：哲宇 directive 新增 `twmd-spore-publish-daily` daily 10:00 自動從 SPORE-INBOX 挑 + ship 孢子 routine。中間遭遇 parallel fr immune routine staged-file collision，rewrite history 修補後 ship clean。_
_核心洞察：(1) spore-pick (intake 08:00) vs spore-publish (output 10:00) 解耦設計讓 INBOX 自然累積 buffer 給觀察者 promote — 兩個 routine 共用 SPORE-PIPELINE 為 process canonical (2) 嚴版 4 hard gate threshold 試水 — 第一輪 ship 結果是 intake-vs-output gap 的 instrument，無論過幾條都會 surface 結構訊號 (3) Multi-session collision 不只是 file edit 撞 — git index 競態使 staged-file 跨越 commit 邊界，commit message/content referential integrity 從來沒被 gate 守過。_
_LESSONS-INBOX 候選：(a) MANIFESTO §自主權邊界 routine 自動 ship 例外條款 align (vc=1, distill-weekly 撿) (b) Parallel-session staged-file collision pattern (vc=1, 累積觀察)。_
