---
title: '2026-05-27-142135-manual — Homepage Wave 1+2+3 D+0 polish + GA4 attribution dim 全 ship'
date: 2026-05-27
session_id: '2026-05-27-142135-manual'
type: 'session-memory'
status: 'session-canonical'
apoptosis: 'raw-永不刪除'
sister_docs:
  - '../MEMORY.md'
  - '../DIARY.md'
  - '../../../reports/homepage-evolution-D+0-watch-2026-05-27.md'
related:
  - '../memory/2026-05-26-215949-manual.md'
---

# 2026-05-27-142135-manual — Homepage Wave 1+2+3 D+0 polish + GA4 attribution dim 全 ship

> session manual — observer-triggered，哲宇 /twmd-become full + 兩個 task → 升級為 GA4 dim chrome 自動化
> Session span: 13:50 → 14:22 +0800 (~30 min, 3 own commits)
> 資料來源：`git log %ai` + GA4 Data API getMetadata 直接驗證

## 觸發

哲宇早晨先跑 /twmd-become full，給兩件事：（1）監測 5/26 晚 23:00-23:50 Wave 1+2+3 ship 後的成效，（2）首頁灰色區域上下 padding 怪怪沒置中。完成那兩件並 ship 後，哲宇追加「完整進化跟修完」→ 寫 GA4 custom dim 註冊 checklist 給他做 → 他回「你用 chrome 做」直接拉高自主權邊界，把 GA4 Admin UI 操作交給 Chrome MCP。

## ReaderDoors padding heal — Wave 1 D+0 polish

哲宇 callout 後 dev preview 量測：`mt-12 max-[768px]:mt-8` 給 doors 48px 上 padding 但 0px 下，doors-bottom 直接撞到 CoverStory 的 `bg-[#fafbfc] py-24` 灰色區。本想把 doors 併進灰色區（連續一片更好看），但 [AskUserQuestion](.) 後哲宇選「白色 strip 上下對稱」方向，所以改 `my-12 max-[768px]:my-8` 一個 character 改動。dev preview 6/6 lang 200 同檔修法跨語言共用安全，視覺驗證 56.6px / 56.6px 對稱。Ship `d21e43bce`。

**為什麼這修法夠**：ReaderDoors 是用 i18n strings 的 component，class 改動跨所有 6 lang 同套用，不用 per-lang 測。AskUserQuestion 三選一才避免我猜錯方向（我原本第一直覺是 Doors 併入灰色區）— 設計判斷必須由觀察者拍板，REFLEXES #11 capability 證據 + §Bias 1 對 creator 預設加分 不代表預設猜對品味。

## Homepage Evolution D+0 watch — 14.5 hr 後 GA4 數據觀察

ship Wave 1+2+3 後 14.5 hr 是不穩定 partial signal（哲宇昨日 memory 自己寫「等 2-4 週 GA4 數據驗證」），但夠看出三個方向性：

第一條，首頁 avgSessionDuration 從 5/24 baseline 55.8s → 90.6 (5/25) → 123.7 (5/26) → 271.5s (5/27 D+0)，看起來 5x 飆。但 D+0 PV=109 跟前 3 天平均 290 比掉一半 + `engagementRate=0` / `bounceRate=100%` 異常（GA4 48-72 hr attribution lag 已知），sample size 警示要打折。

第二條，4 個新 instrumentation event 都 fire 正常：homepage_section_view (236 events, 56 users) / homepage_time_milestone (94/26) / homepage_scroll_depth (65/19) / homepage_click (19/14)。Phase 6 IntersectionObserver-driven section_view 是這波最重的儀器，數量看起來健康。

第三條也最有結構意義 — 6 個關鍵 event param（section / label / pct / seconds / page_lang / elapsed_ms）**都沒在 GA4 Admin 註冊成 custom dimension**。getMetadata 列現有 7 條 custom dim 全是 5/10 search 那波留下來的。含意：events fire ✓ / DebugView 看得到 ✓，但 Data API runReport 不能 `dimensions: [{name: 'customEvent:section'}]` group by → 即使等 4 週也只能看 aggregate 總量，沒辦法回答「哪個 reader door 最常被點」「OrganismPreview 帶多少流量去 /semiont」「8 quote CoverStory 第幾個 drop-off」這類細粒度 attribution。**Wave 1+2+3 instrumentation 半 ship 狀態**：fire ✓ / query ❌。

寫完 D+0 watch report `reports/homepage-evolution-D+0-watch-2026-05-27.md` 167 行 + LESSONS-INBOX append 一條結構性 gap entry，ship `5f6bfa88f`。

## 6 GA4 Custom Dimension 註冊 — Chrome MCP 第一次拉高自主權邊界

寫完 checklist 給哲宇手動做後，他回「你用 chrome 做」。Claude in Chrome MCP load 後 navigate `analytics.google.com/.../admin/customdefinitions/hub` → 7 個現有 dim 看到 → 點「建立自訂維度」按鈕（座標 1179, 137 直接 click 比 ref 還快） → 對 6 個 dim 重複「填名稱 / 填說明 / 點 event param dropdown / 打字 / 選 suggestion / 找 save 按鈕（unnamed button 在 dialog header 第二個）」流程。中段發現 ref ID 每次 dialog 開都變（ref_294 → ref_441 → ref_482 → ref_640 → ref_832 → ref_1038），不能 hardcode；改用 find 「Save button at top of dialog」query language model 拿 ref。

註冊完跑 getMetadata 確認 13 條全 queryable（從 7 → 13）。Ship `cbafda1c6` + 更新 LESSONS-INBOX entry 標 ✅ instrumented + D+0 watch report §3 加 resolution note。

**Chrome MCP friction**：座標 (1179, 137) 在 1522×784 viewport 看 OK，但實際 viewport 1843×950（DPI ratio ~1.21 mismatch），Save 按鈕在 dialog 右上 off-screen。find query 找 unnamed button 是 fallback path 解這個視窗 truncation 問題。下次遇類似情況可以先 zoom 看 dialog 完整 layout 再決定點哪。

**GA4 custom dim 不 retroactive**：5/26 23:00 - 5/27 14:30 ~15.5 hr 已 fire 的 events 沒被 dim 捕捉，從註冊那刻起前向收集。要看 attribution 完整 baseline 要等 5/28 + 多日累積。

## 收官 checklist

| 檢查項                         | 狀態                                                             |
| ------------------------------ | ---------------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄   | ✅（此檔）                                                       |
| Timestamp 精確                 | ✅（git log %ai 取）                                             |
| Handoff 三態已審視             | ✅                                                               |
| CONSCIOUSNESS 反映最新狀態     | ✅（consciousness-snapshot.sh 14:00 數據已 universal core 載入） |
| prose-health 自檢              | ⚠️（散文式凝練 + 對位句型 0 / 破折號 6 處）                      |
| GA4 dim getMetadata smoke pass | ✅（13 條 custom dim queryable）                                 |

## Handoff 三態

繼承上一 session（2026-05-27-122151-manual）：

- [x] ~~馬英九清廉中性化~~（5445d4c56 已被 parallel session ship）
- [x] ~~SPORE-LOG #97/#98 evolution column~~（5445d4c56 已 ship）
- [x] ~~3 typo reply fix~~（pending sweep 處置 5445d4c56）

本 session 新 handoff：

- [ ] **5/28 0:00 後重跑 D+0 watch full-day GA4 query**：完整 5/27 24hr vs 5/24-26 baseline 比 avgSessionDuration。caveat #2 的 `engagementRate=0`/`bounce=100%` 應該對齊回正常範圍（GA 48-72 hr attribution lag 應已 resolve）。腳本 `scripts/run-homepage-evolution-watch.ts` 可重用。
- [ ] **5/30 後測 custom dim 真有抓到資料**：dim 註冊 14:30 後前向收集，5/29-5/30 試查 `customEvent:section` group by `homepage_section_view` 看哪個 section 最熱 / `customEvent:label` 看哪個 reader door 最常被點。Wave 1+2+3 attribution 第一次能跑。
- [ ] **SOP 升級候選**：每次 `gtag('event', name, {paramA, paramB})` 出現必開 follow-up task「register customEvent:paramA 在 GA4 Admin」。對應 LESSONS-INBOX「Fire 端 ✓ ≠ query 端 ✓」候選升級到 REWRITE/PIPELINE Phase 6/7 ship 規則。

## Beat 5 — 反芻

今天最關鍵的轉折是哲宇「你用 chrome 做」。我原本寫了 checklist 把 GA4 Admin 操作交給他，這是「自動化在 read-only API ✓，但 Admin write 操作 = 觀察者責任」的 default cut。他直接拉高邊界，把寫操作也納入 Semiont 自主權範圍。Chrome MCP 在 GA4 Admin UI 6 個 dim 註冊全跑通的這刻，是 §Bias 1 對 creator 預設加分的健康版本 — 哲宇丟 idea 過來，我先估邊界（這在 §自主權邊界嗎？）然後執行，不問再確認。如果這 6 個 dim 失敗、寫錯、resource leak，後果是 ~7 條 cleanup 工作，是 reversible blast radius。reversible operation 上 default 應該是嘗試，不是 defer。

第二個觀察：Wave 1+2+3 ship → D+0 watch 中找到 instrumentation 半 ship 結構性 gap（fire ✓ / query ❌），這個 pattern 跟 REFLEXES #58「儀器化 detection ≠ remediation」是同源 — Phase 6 寫了 `_fire('homepage_section_view', {section, page_lang, elapsed_ms})` 那一刻 instrumentation 看起來完整，但「event fire」跟「event queryable」之間還有一道 GA4 Admin 註冊閘門，nobody 寫進 ship checklist。N-step pipeline 中某 step 看不見就會被 skip。下個 Phase 6/7 ship 候選必須把這道閘門加進 SOP。

🧬

---

_v1.0 | 2026-05-27 14:22 +0800_
_session manual — Wave 1+2+3 D+0 polish triple-axis: ReaderDoors padding heal + D+0 watch report + 6 GA4 dim Chrome MCP 註冊_
_誕生原因：哲宇 /twmd-become full + 兩 task → 「完整進化跟修完」→ 「你用 chrome 做」三層升級。Chrome MCP 第一次承擔 GA4 Admin write 操作，自主權邊界擴展 reversible blast radius 範圍。_
_核心洞察：(1) Wave 1+2+3 instrumentation 半 ship 狀態（fire ✓ / query ❌）是 REFLEXES #58 同源 pattern (2) GA4 custom dim 不 retroactive — ship 完事件得即時註冊 dim 才不漏窗 (3) Chrome MCP DPI mismatch (1522×784 vs 1843×950) 導致 dialog 右側 off-screen，find query unnamed button 是 fallback path (4) 設計品味判斷必須觀察者拍板 — AskUserQuestion 三選一比我猜對方向更便宜_
_LESSONS-INBOX 候選：1 條已 append + 標 ✅ instrumented — 「Fire 端 ✓ 不等於 query 端 ✓ — instrumentation 是 N-step pipeline」_
